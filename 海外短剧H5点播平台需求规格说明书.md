# 海外短剧 H5 点播平台需求规格说明书

> 文档版本：V1.0 MVP
>
> 编写日期：2026-08-07
>
> 项目品牌：ReelNova
>
> 正式主域名：`iseedrama.com`（`www.iseedrama.com` 301 跳转至主域名）
>
> 适用范围：美国市场的移动端 H5 短剧点播平台；东南亚市场作为后续扩展方向
>
> 执行约束：后续 H5 界面、交互、组件和视觉设计均以本文档为单一需求来源；如设计与本文档冲突，应先更新本文档并记录变更。

## 1. 项目概述

### 1.1 项目定位

建设一个面向美国用户的移动端竖屏短剧点播平台。平台支持运营方随时上传、编辑、上架和下架短剧，用户注册或登录后可试看部分剧集，后续通过 PayPal 购买单剧解锁权益后继续观看。

平台首期优先支持美国访问，采用响应式 H5，兼容 iOS Safari、Android Chrome 及常见内嵌浏览器。产品形态以手机竖屏观看为主，同时兼容平板和桌面浏览器的基础浏览。

### 1.2 业务闭环

```text
用户注册或登录
  -> 进入首页
  -> 浏览分类和短剧卡片
  -> 进入剧集详情
  -> 免费观看试看集
  -> 播放锁定集时弹出 PayPal 支付
  -> 完成支付并收到回调
  -> 解锁对应短剧全部可售剧集
  -> 播放、记录观看进度
  -> 用户中心查看订单、已购剧集和支付记录
  -> 管理后台持续上新和运营配置
```

### 1.3 MVP 目标

- 支持短剧内容上传、转码、封面管理和上架。
- 支持 5-6 个首页内容分区及分类标签配置。
- 用户必须注册或登录后才能进入前台业务界面和观看试看内容。
- 支持 PayPal Checkout 支付、支付回调、订单状态同步和退款状态记录。
- 支持按单剧购买解锁，首期不做订阅和复杂会员体系。
- 支持已购权益跨设备恢复，使用用户账号和邮箱关联订单。
- 支持可配置域名，域名更换不影响内容、订单和用户权益数据。
- 支持基础防盗播、播放地址签名和访问频控。

### 1.4 非目标

- 首期不建设原生 iOS/Android App。
- 首期不支持直播、用户上传内容、评论社区、私信和社交关注。
- 首期不支持多支付渠道并行结算；支付主渠道为 PayPal。
- 首期不做复杂广告投放平台和推荐算法训练平台。
- 首期不承诺完全阻止录屏，防盗播目标为降低资源直链和批量下载风险。

## 2. 用户角色和权限

| 角色 | 主要能力 |
| --- | --- |
| 注册用户 | 登录后浏览首页、分类、详情、观看试看集、发起支付和查看观看记录 |
| 已购用户 | 恢复订单、查看已购短剧、继续观看、查看观看记录和支付记录 |
| 内容运营 | 上传视频、维护剧集、维护封面和文案、批量上架/下架、配置分类和标签 |
| 财务/运营管理员 | 查看订单、支付流水、退款状态、导出对账数据、配置价格和优惠活动 |
| 超级管理员 | 管理管理员账号、域名、PayPal 配置、系统参数、审计日志 |

权限采用最小化原则，后台所有高风险操作（删除内容、修改 PayPal 配置、退款标记、域名切换）必须记录操作人、时间、IP、变更前后值。

## 3. 市场、地区和语言要求

### 3.1 地区

- 首期目标地区：美国（United States）。
- 默认地区为 `US`，默认语言为美式英语 `en-US`，默认货币为美元 `USD`。
- 根据 IP 提供美国地区内容排序和可见性；后续扩展其他国家时再增加地区切换。
- 受版权或合规限制的剧集可按国家/地区配置可见性。

### 3.2 语言

- MVP 仅支持美式英语 `en-US`，前台默认展示英文文案、英文分类和英文支付提示。
- 文案需使用美国市场常用表达，例如 `Watch Free`、`Unlock Full Series`、`Continue Watching`、`Payment Processing`。
- 架构需预留简体中文、繁体中文、泰语、越南语、印尼语和马来语扩展能力，但不纳入当前 MVP 设计和验收范围。
- 剧名、简介、分类、价格提示、支付提示和错误提示均使用可配置翻译资源，不得硬编码在页面组件中。

### 3.3 货币和价格

- 默认且首期唯一展示货币为 USD。
- 所有价格以美元结算，PayPal 订单币种固定为 `USD`。
- 所有价格展示必须包含美元符号或 ISO 代码，例如 `$2.99` 或 `$2.99 USD`。
- 价格、折扣、税费和最终支付金额在下单前明确展示。

## 4. 信息架构和导航

### 4.1 前台底部导航

1. Home：推荐、Popular、New、Rankings、Categories。
2. Explore：全部分类、标签筛选、搜索和排序。
3. Library：已购短剧、收藏（MVP 可隐藏入口，数据结构预留）。
4. Profile：订单记录、支付记录、观看记录、语言和帮助。

底部导航固定在手机底部安全区域内，支持 iPhone 刘海屏和 Android 手势条。当前页面以高对比色图标和文字标识，点击区域不小于 44x44 px。

### 4.2 首页 5-6 个内容分区

首页分区由后台配置顺序、显示数量和内容来源，默认如下：

| 分区 | 内容逻辑 | 典型标签 |
| --- | --- | --- |
| Popular | 近 7/30 天播放量或完播量最高 | Popular |
| New Releases | 最近上架或更新的短剧 | New |
| Rankings | 播放量、付费转化或收入排行 | Top 10、Trending |
| Romance | 爱情、甜宠、虐恋等 | Romance |
| Revenge & Power | 复仇、逆袭、豪门等 | Revenge、Billionaire |
| US Picks | 针对美国观众的内容和热门短剧 | US Picks、Trending in the US |

实际启用 5 或 6 个分区均可，后台可拖拽排序、隐藏和修改标题。每个分区至少支持横向滑动卡片列表，点击卡片进入剧集详情。

## 5. 前台 H5 功能需求

### 5.1 首页

#### 页面目标

让用户快速发现短剧并进入播放。首屏必须出现分类导航、至少一排短剧卡片和明确的播放量/标签信息。

#### 页面结构

- 顶部：品牌 Logo、搜索入口、语言/地区入口；可保留系统状态栏空间。
- 分类导航：Popular、New、Rankings、Categories，支持横向滚动，当前项有底部指示条。
- 内容分区：每个分区包含标题、查看更多入口和卡片列表。
- 卡片：竖版封面、Hot/New/Exclusive 标签、剧名、类型、播放量、更新集数。
- 底部：Home、Explore、Library、Profile。

#### 卡片规则

- 默认 3 列竖屏卡片，卡片宽度随容器自适应，封面比例建议 2:3。
- 封面必须有渐变遮罩或底部信息区，保证播放量文字可读。
- 播放量支持 1.2K、3.4M 等缩写，原始数值保留在数据层。
- Hot、New、Free、Paid 标签使用不同颜色和文案，标签位置固定，不得遮挡关键人物主体。
- 卡片标题最多显示 2 行，超出截断；不得造成布局跳动。
- 加载中显示骨架屏；无数据显示分区空状态和刷新入口。

#### 首页行为

- 下拉刷新；分页或分区懒加载。
- 点击导航切换对应内容，不整页刷新。
- 点击卡片进入详情；返回后保留原滚动位置。
- 记录曝光、点击、播放开始、试看转付费等埋点。

### 5.2 分类/探索页

- 支持按类型、语言、标签筛选；首期语言固定为 English，地区固定为 United States。
- 支持 Popular、Newest、Most Watched、Most Purchased 排序。
- 支持关键词搜索剧名、演员名和标签。
- 无结果时展示空状态、推荐分类和清除筛选按钮。
- 筛选条件可通过 URL 查询参数保存，便于分享和返回恢复。

### 5.3 剧集详情页

#### 页面结构

- 顶部返回、分享、收藏（如 MVP 暂不上线则隐藏）。
- 主视觉：竖版封面、剧名、标签、播放量、评分/热度（如配置）。
- 简介：一句话卖点、完整简介、演员/题材信息。
- 主按钮：Continue Watching、Watch Free、Unlock Now，根据用户状态动态显示。
- 分集列表：按集数排列，展示集数、时长、试看/已购/锁定状态。
- 推荐短剧：同分类或后台指定的相关内容。

#### 试看规则

- 每部剧由后台配置 `free_episode_count`，默认前 3 集免费，可设置为 0-10 集。
- 试看集不额外收费，但仅向已注册并登录的用户开放，播放记录关联用户账号。
- 同一设备可限制试看时长、试看集数或播放频次，防止批量滥用。
- 试看结束时在播放器内提示“Unlock to continue”，并提供价格和 PayPal 支付入口。

#### 购买规则

- MVP 采用“单剧整剧解锁”，购买后解锁该剧所有已上架及后续新增剧集；后台可配置是否包含后续更新。
- 价格支持原价、活动价、地区价和有效期。
- 未支付订单可继续支付；已支付订单不可重复扣款，重复点击应返回已购状态。
- 详情页必须明确：可免费观看几集、购买后解锁范围、价格、退款/客服说明。

### 5.4 播放页

#### 播放器能力

- 默认竖屏播放器，适配 9:16 内容；横屏视频可居中留黑边，不裁切主体。
- 支持播放/暂停、进度拖动、音量、清晰度（如源文件支持）、倍速、全屏、下一集。
- 支持 HLS（`.m3u8`）播放，优先使用移动端原生能力，Web 端使用兼容播放器。
- 记忆最近播放位置和最近观看集数，用户回到详情页可看到 Continue Watching。
- 网络中断自动重试，失败时显示重试按钮和可读错误文案。

#### 付费锁片机制

- 播放前端不得仅依赖隐藏控件判断权限；每次获取播放地址均由服务端校验权益。
- 锁定集显示锁图标和解锁 CTA，不返回可用的视频源地址。
- 播放过程中权益失效、订单待确认或签名过期时暂停播放并提示刷新/支付状态。
- 播放地址采用短时有效签名 URL，建议有效期 5-15 分钟，可按需刷新。

#### 账号访问逻辑

- 未登录用户访问任一前台业务页面时，统一跳转至登录/注册页面，并在成功后返回原目标页面。
- 登录用户可播放试看集；支付、订单和权益必须关联当前用户账号。
- 用户清理浏览器缓存后，可通过订单邮箱、订单号或 PayPal 交易号恢复权益。
- 恢复权益需要二次校验，禁止仅凭前端传入的邮箱直接解锁。

### 5.5 PayPal 付费解锁弹窗

- 弹窗包含剧名、封面缩略图、解锁范围、原价/活动价、币种、服务条款链接和 PayPal 支付按钮。
- 支付按钮使用 PayPal 官方 SDK 或官方托管按钮，不自绘仿冒 PayPal 按钮。
- 支付状态分为：待支付、处理中、成功、失败、取消、退款中、已退款。
- 支付成功以后优先等待服务端回调确认；前端轮询订单状态，确认后刷新详情和播放器权限。
- 支付失败保留订单，允许重试；取消支付返回详情页且不改变剧集权益。
- 在美国网络环境下，支付加载超时必须给出重试和联系客服入口。

### 5.6 用户中心

用户中心仅支持已登录用户访问，并根据是否已购买内容展示不同数据状态。

#### 未购买状态

- 展示当前账号的观看记录和订单查询入口。
- 提供“恢复购买”功能，可输入订单邮箱或订单号。
- 展示真实账号信息，不展示虚构的等级或余额信息。

#### 已购状态

- 已购短剧：封面、剧名、购买时间、观看进度、继续观看按钮。
- 订单记录：订单号、剧名、金额、币种、状态、创建时间、PayPal 交易号。
- 支付记录：成功、失败、退款等状态及详情。
- 观看记录：最近观看集数、进度、最近播放时间，支持清除。
- 设置：语言、地区、隐私、客服、服务条款、退款政策。

## 6. 视觉和交互设计基线

后续界面设计需对标用户提供的参考截图，但不直接复制第三方品牌资产。

### 6.1 视觉方向

- 移动端优先，默认深色沉浸式观影氛围；页面背景接近黑色，内容区域使用高对比文字。
- 主色用于当前导航、Hot 标签和关键 CTA；支付按钮必须遵循 PayPal 品牌规范。
- 采用竖版海报密集陈列，突出人物、剧名和热度数据。
- 信息层级优先级：封面和剧名 > 免费/付费状态 > 播放量/更新信息 > 分类描述。
- 圆角、阴影和动效保持克制，不使用影响阅读的装饰性大图形。

### 6.2 响应式要求

- 重点适配 320-430 px 宽度手机屏幕，兼容 768 px 平板和桌面基础布局。
- 页面使用 CSS safe-area，底部导航和播放器控件不得被系统手势区域遮挡。
- 所有按钮最小触控区域 44x44 px；文字不得溢出、重叠或挤压相邻内容。
- 竖屏播放为默认状态；旋转到横屏时可切换沉浸式全屏。

### 6.3 组件状态

每个核心组件都必须设计默认、加载、空数据、错误、禁用、已购买、已锁定和网络重试状态。支付弹窗还必须覆盖处理中和回调延迟状态。

## 7. 后台管理系统需求

### 7.1 剧集管理

#### 剧集字段

- 剧集 ID、剧名、英文名、别名。
- 主封面、列表封面、横幅图、缩略图。
- 简介、短标题、演员、导演、版权信息。
- 分类、标签、目标地区、语言、字幕语言。
- 剧集状态：草稿、处理中、待发布、已上架、已下架、版权冻结。
- 免费试看集数、总集数、单集时长、解锁模式。
- 原价、活动价、地区价格、上架时间、下架时间。
- SEO 标题、描述、分享图和 URL slug。

#### 上传和发布

- 支持单集视频上传、批量上传和断点续传。
- 上传后校验格式、大小、时长和音视频轨道，展示转码进度。
- 支持生成 HLS 多码率文件、封面截图和预览图。
- 支持批量编辑分类、标签、试看集数和价格。
- 支持定时上架、立即上架、下架和恢复上架。
- 发布前提供预览，确认后才对前台可见。
- 删除采用软删除，已产生订单的剧集不得物理删除。

### 7.2 内容运营配置

- 首页分区：新增、编辑、隐藏、排序、配置内容来源和数量。
- 分类管理：分类名称、多语言名称、图标/封面、排序、启用状态。
- 标签管理：Hot、New、Exclusive、Free 等标签的颜色、文案和有效期。
- 推荐位管理：指定剧集、推荐权重、开始/结束时间。
- 活动管理：折扣码、活动价格、适用地区、有效时间、使用次数上限。
- 全局配置：站点名称、Logo、客服邮箱、服务条款、隐私政策、退款政策。

### 7.3 用户管理

- 查看用户 ID、关联邮箱、注册时间、最近访问时间、地区和设备信息。
- 查看观看记录、已购权益、订单和退款状态。
- 支持禁用异常账号、解除设备限制、手工补发权益；所有操作写入审计日志。
- 不在后台明文展示完整支付敏感信息。

### 7.4 支付与财务

- PayPal 沙箱/生产环境切换，Client ID、Secret 使用加密存储。
- 支持订单列表、订单详情、状态筛选、地区筛选、日期筛选和导出 CSV。
- 记录本地订单号、PayPal Order ID、Capture ID、币种、金额、手续费（如可得）、净额和回调时间。
- 支持 PayPal Webhook 接收、签名验证、幂等处理和失败重试。
- 对账页面支持按日期和币种汇总订单金额、成功支付数、退款数、异常订单数。
- 后台可标记客服处理结果，但不得绕过支付事实直接伪造支付成功。

### 7.5 域名管理

- 业务数据、对象存储、支付配置和域名解耦，域名仅作为访问入口。
- 后台支持配置主域名、备用域名和强制跳转策略。
- 支持自定义域名绑定、DNS 配置提示、HTTPS 证书状态和域名验证状态。
- 更换域名后旧域名可配置 301 跳转或展示迁移页。
- 分享链接使用稳定的内容 slug 或内部 ID，不把域名写入业务数据。
- PayPal 回调地址和域名白名单变更必须有发布检查清单，避免换域名后支付回调失效。

## 8. 支付系统详细要求

### 8.1 下单流程

1. 前端请求创建订单，服务端校验剧集状态、价格版本和登录用户身份。
2. 服务端创建本地订单，状态为 `pending`，生成唯一订单号。
3. 服务端调用 PayPal 创建订单，保存 PayPal Order ID。
4. 前端打开 PayPal 审批流程。
5. 用户完成授权后，服务端执行 capture 或接收 PayPal Webhook。
6. 服务端验证金额、币种、商户账号和订单关联关系。
7. 订单更新为 `paid`，发放剧集权益，写入支付流水。
8. 前端轮询订单状态并刷新可播放集数。

### 8.2 幂等和异常

- 同一业务订单只能对应一次有效权益发放。
- Webhook、前端回调和后台重试必须幂等。
- 金额或币种不一致时订单标记为 `risk_review`，不自动解锁。
- PayPal 成功但本地回调延迟时，前端展示“Payment processing”，后台自动补偿查询。
- 退款后权益处理策略可配置：立即锁定、保留到当前观看结束或人工审核；MVP 默认退款确认后锁定。

### 8.3 退款和客服

- 前台展示退款政策和客服邮箱。
- MVP 可由后台人工发起或记录退款，PayPal 实际退款动作需使用官方接口或 PayPal 商户后台完成。
- 退款状态与权益状态分离，确保对账和用户展示一致。

## 9. 视频分发和基础防盗播

### 9.1 推荐链路

```text
对象存储（原始视频）
  -> 转码服务（HLS，多码率）
  -> CDN 分发
  -> 服务端签名播放地址
  -> H5 播放器
```

### 9.2 视频要求

- 接受 MP4/MOV 等常见源文件，统一转码为 H.264/AAC HLS。
- 至少提供 480p、720p 两档；根据带宽自适应。
- 视频、字幕、封面和海报使用独立对象存储路径。
- 原始视频不允许通过公开 URL 访问。

### 9.3 防盗播基础能力

- CDN 私有回源和短时签名 URL。
- 播放地址绑定剧集、集数、登录用户会话和过期时间。
- Referer、Origin、IP/设备频率等基础校验；对异常请求限流。
- 播放接口不返回原始存储地址。
- 记录播放会话、并发设备数和异常下载行为。
- 可选添加用户订单号或用户标识水印，作为后续增强能力。

## 10. 多地区访问和性能要求

- 首期 CDN 节点优先覆盖美国主要区域，后续扩展新加坡、香港、日本及东南亚节点。
- 首页首屏接口目标：4G 网络下 P75 小于 2.5 秒；首张封面优先加载。
- 播放首帧目标：普通 4G 网络下 P75 小于 5 秒。
- 图片使用 WebP/AVIF，按设备尺寸返回合适分辨率。
- API 支持超时、重试和降级；支付页面失败不影响已购内容浏览。
- 记录按国家/地区、设备、浏览器、CDN 节点的访问和播放质量指标。

## 11. 数据模型（MVP）

### 11.1 核心实体

| 实体 | 关键字段 |
| --- | --- |
| `series` 剧集 | id、slug、title、description、cover_url、status、free_episode_count、price、currency、published_at |
| `episode` 分集 | id、series_id、episode_no、title、duration、video_status、hls_path、is_free |
| `category` 分类 | id、name、locale、sort_order、enabled |
| `tag` 标签 | id、name、color、enabled |
| `user` 用户 | id、email、password_hash、status、locale、country、device_summary、created_at |
| `order` 订单 | id、order_no、user_id、series_id、amount、currency、status、paypal_order_id |
| `payment_event` 支付事件 | id、order_id、event_type、paypal_event_id、payload_hash、processed_at |
| `entitlement` 权益 | id、owner_id、series_id、source_order_id、status、valid_from、valid_until |
| `watch_history` 观看记录 | owner_id、series_id、episode_id、progress_seconds、last_watched_at |
| `admin_audit_log` 审计日志 | admin_id、action、resource_type、resource_id、before_json、after_json、ip、created_at |

### 11.2 关键约束

- `order_no`、PayPal Order ID、Capture ID、Webhook Event ID 唯一。
- 同一拥有者对同一剧集最多一条有效权益。
- 订单金额以创建订单时的价格快照为准，后续改价不影响历史订单。
- 内容下架不影响已购权益展示，但可根据版权策略禁止播放并展示说明。

## 12. API 边界（建议）

### 12.1 前台 API

- `GET /api/home`：首页分区和卡片。
- `GET /api/categories`：分类和标签。
- `GET /api/series/{slug}`：剧集详情、分集和用户状态。
- `GET /api/series/{id}/episodes/{episodeNo}/playback`：校验权益并返回签名播放地址。
- `POST /api/auth/register`：注册用户并创建登录会话。
- `POST /api/auth/login`：校验账号并创建登录会话。
- `POST /api/auth/logout`：清除登录会话。
- `GET /api/auth/session`：获取当前登录用户。
- `POST /api/orders`：创建 PayPal 订单。
- `GET /api/orders/{orderNo}`：查询订单状态。
- `POST /api/orders/{orderNo}/restore`：恢复购买。
- `GET /api/me/library`：已购短剧。
- `GET/POST /api/me/watch-history`：观看记录读写。

### 12.2 管理 API

- `POST /admin/api/series`、`PATCH /admin/api/series/{id}`：剧集管理。
- `POST /admin/api/episodes/upload`：获取上传凭证。
- `POST /admin/api/series/{id}/publish`、`POST /unpublish`：上架/下架。
- `GET /admin/api/orders`、`GET /admin/api/reconciliation`：订单和对账。
- `CRUD /admin/api/categories`、`CRUD /admin/api/tags`：分类标签。
- `CRUD /admin/api/home-sections`：首页分区。
- `GET/PATCH /admin/api/site-config`：域名、Logo、客服和全局配置。
- `POST /api/paypal/webhook`：PayPal Webhook 接收端点。

所有 API 必须返回统一错误结构，包含 `code`、`message`、`request_id`；敏感字段不得返回给前端。

## 13. 安全、合规和隐私

- 全站 HTTPS，后台启用强密码和二次验证预留能力。
- PayPal Secret、对象存储密钥和签名密钥使用密钥管理服务或加密环境变量保存。
- Webhook 必须校验 PayPal 签名，拒绝未验证来源。
- 采集最少化个人信息；账号邮箱仅用于登录、订单恢复和客服。
- 提供隐私政策、服务条款、退款政策、版权投诉入口和客服邮箱。
- 按地区配置年龄提示、内容分级和不可售区域。
- 日志脱敏，不记录完整支付卡片信息或 PayPal 密钥。
- 支持账号/订单数据导出和删除请求的人工处理流程。

## 14. 埋点和运营指标

### 14.1 必备埋点

- 首页分区曝光、卡片曝光、卡片点击。
- 详情页打开、试看开始、试看完成、锁片触发。
- 支付弹窗打开、PayPal 点击、支付成功、支付失败、支付取消。
- 播放开始、首帧、播放中断、播放完成、下一集点击。
- 搜索、筛选、分享、恢复购买。

### 14.2 核心指标

- 首页到详情点击率。
- 试看开始率、试看完成率、锁片转化率。
- 支付成功率、支付回调延迟、退款率。
- 单剧收入、地区收入、内容播放量和完播率。
- 播放首帧时间、卡顿率、播放失败率。

## 15. MVP 验收标准

### 15.1 前台

- 用户可在手机竖屏访问首页，看到 5-6 个分区、分类导航和短剧卡片。
- 卡片正确展示封面、剧名、Hot/New 标签和播放量，并可进入详情。
- 未登录访问首页、分类、详情、播放、片库或用户中心时，必须跳转至登录/注册页面。
- 用户注册或登录后，可按配置播放每部剧的试看集。
- 播放锁定集时弹出 PayPal 支付弹窗，展示准确价格和解锁范围。
- 支付成功且回调确认后，用户可播放已解锁剧集；刷新页面后权益仍存在。
- 用户可在用户中心查看订单、已购短剧、观看记录和支付状态。
- 关键页面在 320 px 宽度下无横向溢出、文字重叠和底部导航遮挡。

### 15.2 后台

- 管理员可新增剧集、批量上传分集、维护封面和文案。
- 管理员可设置试看集数、价格、活动价、分类、标签和上架时间。
- 管理员可预览、上架、下架和恢复短剧。
- 管理员可查询订单、支付状态、退款状态并导出对账数据。
- 管理员可调整首页分区顺序和内容。
- 管理员可切换 PayPal 沙箱/生产配置并查看 Webhook 状态。
- 管理员可配置主域名和备用域名，域名变更不丢失历史数据。

### 15.3 系统

- 视频源文件不公开，播放地址为短时签名 URL。
- 重复 Webhook 不重复发放权益。
- 支付金额与本地订单金额不一致时不自动解锁。
- 美国目标网络下可完成首页访问、试看、支付和已购播放闭环。
- 所有高风险后台操作有审计日志。

## 16. 测试要求

- 功能测试：注册、登录、未登录拦截、登录后试看、支付成功/失败/取消、回调重复、退款、恢复购买、上下架。
- 兼容性测试：iOS Safari、Android Chrome、主流微信/社交内嵌浏览器、桌面 Chrome/Safari。
- 网络测试：4G、弱网、断网恢复和美国节点。
- 安全测试：越权播放、篡改订单金额、伪造回调、重复发放权益、播放地址复用。
- 性能测试：首页首屏、详情接口、播放首帧、并发播放和批量上传。
- 可用性测试：新用户可顺利完成注册或登录，并在认证后完成试看和支付；已购用户可恢复内容。

## 17. 交付阶段建议

### 阶段 1：产品和视觉基线

- 完成信息架构、移动端 UI、组件库、设计 token、页面状态和英文文案。
- 产出首页、详情、播放器、支付弹窗、用户中心和后台核心页面设计稿。

### 阶段 2：MVP 开发

- 完成内容管理、HLS 转码、CDN 播放、用户注册登录会话、订单和 PayPal 支付闭环。
- 完成首页分区、分类筛选、详情、播放和用户中心。

### 阶段 3：联调和上线

- PayPal 沙箱联调、Webhook 验证、真实设备和多地区网络测试。
- 配置生产域名、HTTPS、CDN、监控、日志、备份和应急回滚。

### 阶段 4：运营增强

- 增加多语言、多币种、折扣码、推荐策略、收藏、评论和水印能力。

## 18. 待确认事项

以下事项在进入开发前必须由业务方确认，并写入版本变更记录：

- PayPal 美国商户主体注册国家、收款账户和 USD 结算能力。
- 每部剧默认价格、地区价格和折扣活动规则。
- 试看集数及“购买后包含后续更新”的权益策略。
- 版权覆盖国家、内容分级和下架处理规则。
- 首期正式支持语言确认：美式英语 `en-US`。
- 视频存储、转码和美国 CDN 服务商及预算上限。
- 是否在现有邮箱密码登录之外增加 Magic Link 或第三方登录。
- 退款时限、客服 SLA 和人工补发权益流程。
- 正式主域名已确认为 `iseedrama.com`；`www.iseedrama.com` 作为兼容入口并 301 跳转至主域名。其他备用域名和域名切换审批人仍待业务方确认。

## 19. 设计执行说明

后续 H5 界面设计必须遵循本章节：

1. 首屏优先呈现短剧内容，不制作与点播无关的营销型落地页。
2. 首页采用深色、高密度竖版海报网格，参考截图中的 Popular/New/Rankings/Categories 导航和 Hot/New 标签，但使用自有品牌、原创文案和合法内容素材。
3. 详情页和播放页必须同时表现“免费试看”和“付费解锁”两种状态，不能只设计支付成功态。
4. 支付弹窗必须包含加载、处理中、失败、取消、成功和回调延迟状态。
5. 每个页面都需要提供移动端安全区、弱网、空状态、错误状态和已购状态。
6. 设计稿中的价格、集数、播放量、剧名和标签长度需按真实数据进行溢出测试。
7. 组件命名、颜色、间距、字号和交互状态应沉淀为可复用设计 token，便于后续换肤和更换域名后的品牌配置。
