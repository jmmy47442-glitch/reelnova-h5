# 海外短剧 H5 点播平台项目与需求缺陷审计

> 审计日期：2026-08-25  
> 审计对象：当前仓库代码、`海外短剧H5点播平台需求规格说明书.md`、本地/远程只读配置检查  
> 结论口径：P0 = 上线阻塞或严重安全风险；P1 = 核心流程、数据或验收受影响；P2 = 一般功能、体验或工程质量问题

## 1. 结论

当前项目已经具备可构建的 Nuxt 3 H5、后台 RBAC、D1 内容/订单/权益模型、PayPal 状态处理、R2/Stream 媒体链路和正式域名，但**不满足生产上线条件，也不能按当前需求文档直接判定 MVP 验收通过**。

本次确认的主要问题如下：

- 4 项 P0：任意账号密码可被重置、生产安全配置存在危险默认值、依赖含 critical/high 漏洞、PayPal Production 未配置。
- 11 项 P1：购买恢复入口与接口不一致、无登录/播放频控、播放令牌未绑定用户会话、地区版权规则未执行、探索排序和分区跳转错误、后台内容字段与运营能力不完整、首页配置来源不生效、分析接口被 RBAC 拒绝、可视化验收脚本失效、隐私删除非原子、没有交易型验收数据。
- 4 项 P2：播放器存在空按钮、展示虚构实时人数、MVP 语言范围漂移、自动化测试覆盖不足。
- 需求文档本身有 8 类缺陷，主要集中在未决业务规则、账号安全、版权地区模型、可量化验收口径和接口/数据模型不一致。

## 2. 验证结果

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| `npm run typecheck` | 通过 | 无 TypeScript 类型错误 |
| `npm run build` | 通过 | Node 生产构建完成；有静态海报运行时解析警告 |
| `npm run test:media-worker` | 通过 | 4/4 |
| `npm run test:entitlements` | 通过 | 6/6 |
| `npm run test:payments` | 通过 | 5/5 |
| `npm run visual-check` | **失败** | 用户登录仍使用旧协议，`POST /api/auth/login` 返回 400 |
| `npm audit --json` | **失败** | 共 14 项：2 critical、6 high、3 moderate、3 low |
| `npm run check:production` | **阻塞** | 缺少 PayPal Production 的 4 项凭证 |
| `npm run check:acceptance-data` | 部分通过 | 5 部剧、7 集、6 个首页分区；支付/退款/权益回收/观看历史数据均为 0 |
| `npm run check:domains` | 通过 | 主站、后台、媒体、www 的 DNS/TLS 正常；www 301 保留路径和查询参数 |

说明：未执行会写入远程环境的迁移、播种、上传、真实支付、退款或部署操作。

## 3. P0：上线阻塞与严重安全缺陷

### P0-01 任意账号密码可被未授权重置

**证据**

- [`server/api/auth/password-reset.post.ts:5`](./server/api/auth/password-reset.post.ts#L5) 只接收邮箱、新密码及客户端生成的哈希。
- [`server/api/auth/password-reset.post.ts:15`](./server/api/auth/password-reset.post.ts#L15) 直接调用密码更新，没有验证码、邮件链接、一次性令牌、旧密码或已登录会话校验。
- [`server/utils/user-auth.ts:167`](./server/utils/user-auth.ts#L167) 查询到邮箱后直接更新密码。
- 接口还以 404/成功区分邮箱是否存在，可被用于账号枚举。

**影响**：攻击者只要知道注册邮箱即可接管用户账号，并访问订单、观看记录和已购权益。

**修复要求**：立即关闭该端点或实现一次性、短有效期、单次消费的邮件重置令牌；成功重置后撤销全部旧会话，并统一对外响应避免枚举。

### P0-02 生产认证配置允许回退到公开默认值

**证据**

- [`nuxt.config.ts:59`](./nuxt.config.ts#L59) 内置 `admin@reelnova.com / ReelNova@2026`。
- [`nuxt.config.ts:62`](./nuxt.config.ts#L62) 管理员凭据加密、管理员会话和用户会话均有固定回退密钥。
- [`server/utils/admin-auth.ts:141`](./server/utils/admin-auth.ts#L141) 会在账号不存在时根据上述配置自动创建超级管理员。
- 当前本地 `.env` 未配置 `SUPER_ADMIN_*`、`ADMIN_*_SECRET`、`USER_SESSION_SECRET`；[`scripts/check-production-integrations.mjs:19`](./scripts/check-production-integrations.mjs#L19) 的生产门禁也不检查这些变量。

**影响**：部署环境漏配时可能自动创建公开凭据的超级管理员，且固定会话密钥可导致伪造会话或无法可靠轮换。

**修复要求**：生产环境缺少强随机密钥或超级管理员初始化信息时拒绝启动/部署；删除生产回退值；初始化管理员改为显式一次性命令。

### P0-03 生产依赖存在已公开严重漏洞

**证据**

- [`package.json:31`](./package.json#L31) 固定 `element-plus@2.9.1`。
- [`package.json:35`](./package.json#L35) 固定 `nuxt@3.15.4`。
- `npm audit --json` 报告 14 项漏洞，其中 2 critical、6 high；直接依赖 Nuxt 为 high、Element Plus 为 moderate。
- Nuxt 链路包含服务端 RCE、未认证资源耗尽、路由规则绕过等公告；审计建议升级到 `nuxt@3.21.11`，Element Plus 可升级到 `2.14.5`。

**影响**：存在远程执行、拒绝服务、XSS 或授权绕过风险；即使部分漏洞只影响开发服务器，也不能在未分级确认前上线。

**修复要求**：升级依赖并重新执行类型检查、构建、全部测试和页面回归；记录每项仍无法消除的漏洞是否可达及补偿措施。

### P0-04 PayPal Production 当前未配置

**证据**

`npm run check:production` 当前明确阻塞以下变量：

- `PAYPAL_PRODUCTION_CLIENT_ID`
- `PAYPAL_PRODUCTION_SECRET`
- `PAYPAL_PRODUCTION_WEBHOOK_ID`
- `NUXT_PUBLIC_PAYPAL_PRODUCTION_CLIENT_ID`

对应门禁逻辑见 [`scripts/check-production-integrations.mjs:19`](./scripts/check-production-integrations.mjs#L19)。

**影响**：无法完成需求要求的生产支付、回调确认、权益发放和退款闭环。

**修复要求**：配置生产商户凭据，确认浏览器/服务端 Client ID 一致，完成 OAuth、Webhook 签名、成功/取消/失败/重复回调/退款实测后再放行。

## 4. P1：核心功能与验收缺陷

### P1-01 “按邮箱恢复购买”实际上不可用

- UI 明确提示可输入邮箱：[`pages/profile.vue:52`](./pages/profile.vue#L52)。
- 接口仅识别订单号，否则将输入当作 PayPal Order ID 或 Capture ID：[`server/api/orders/restore.post.ts:12`](./server/api/orders/restore.post.ts#L12)、[`server/api/orders/restore.post.ts:21`](./server/api/orders/restore.post.ts#L21)。
- 输入邮箱会进入 PayPal/Capture 查询分支，因此正常情况下恒定查不到。

建议统一产品规则：若允许邮箱恢复，必须通过当前登录邮箱、邮件验证码或 PayPal 身份二次验证；若不允许，应删除 UI 和多语言文案中的邮箱选项。

### P1-02 登录、注册和密码接口没有服务端频控

认证端点仅做格式与 proof 校验，未发现按 IP、账号、设备的失败计数、退避、验证码或封禁策略，见 [`server/api/auth/login.post.ts:4`](./server/api/auth/login.post.ts#L4) 和 [`server/api/admin/auth/login.post.ts:5`](./server/api/admin/auth/login.post.ts#L5)。后台登录同样没有频控。

影响包括暴力破解、账号枚举、D1/CPU 消耗攻击；客户端 PBKDF2 不等于服务端限流。

### P1-03 播放令牌未绑定用户/会话，且没有并发设备限制

- 权益校验发生在播放地址签发前，这是正确的：[`server/api/playback.get.ts:52`](./server/api/playback.get.ts#L52)。
- 但 Cloudflare Stream 令牌只以 `uid + exp` 申请：[`server/utils/media-pipeline.ts:187`](./server/utils/media-pipeline.ts#L187)，没有用户、播放会话、剧集或分集约束。
- 用户和会话只绑定在播放埋点 token：[`server/api/playback.get.ts:82`](./server/api/playback.get.ts#L82)，不能阻止已复制的 Stream URL 在有效期内被他人使用。
- 未发现每 IP/设备的播放频控、并发会话计数或异常下载检测。

这不满足需求 9.3 的“绑定剧集、集数、登录用户会话”和访问频控要求。需明确 Cloudflare Stream 的能力边界，并至少增加短 TTL、会话并发控制、令牌复用监控和可撤销策略。

### P1-04 地区可见性仅存储、不执行

- 后台保存 `targetRegion`：[`pages/admin/series.vue:440`](./pages/admin/series.vue#L440)。
- 公共内容查询只按“已上架”过滤，并在输出前直接丢弃地区字段：[`server/utils/managed-content.ts:196`](./server/utils/managed-content.ts#L196)。
- 首页、探索和详情接口没有按请求国家或版权地区过滤。

版权受限剧集会被所有地区用户看到并可能购买/播放，违反需求 3.1、9.3 和 13 的地区控制要求。

### P1-05 探索页分区跳转和排序逻辑错误

- 首页“查看更多”生成 `/explore?section=...`：[`pages/index.vue:102`](./pages/index.vue#L102)，探索页只读取 `q/genre/sort`，完全忽略 `section`：[`pages/explore.vue:10`](./pages/explore.vue#L10)。
- “Most Purchased”按当前用户的 `purchased` 布尔值排序：[`pages/explore.vue:30`](./pages/explore.vue#L30)，不是全站购买量。
- 分类列表硬编码：[`pages/explore.vue:14`](./pages/explore.vue#L14)，后台新增、停用或排序分类不会同步到前台。
- 所有数据一次性加载后在浏览器过滤，没有需求要求的分页/懒加载能力。

### P1-06 后台剧集字段和运营能力明显少于需求

当前新建/编辑只支持剧名、分类、目标地区、试看集数、价格和简介，见 [`pages/admin/series.vue:437`](./pages/admin/series.vue#L437) 与 [`server/utils/admin-content-input.ts:3`](./server/utils/admin-content-input.ts#L3)。以下需求无法维护：

- 别名、短标题、演员、导演、版权信息、语言/字幕语言。
- 主封面、列表封面、横幅、分享图、SEO 标题/描述。
- 原价/活动价/地区价和有效期。
- 标签关联、解锁后是否包含未来更新。
- 定时上/下架时间；数据库有 `scheduled_at`，但页面和 API 没有日期输入或调度器。
- 活动管理、折扣码、推荐权重、站点 Logo、客服邮箱和政策内容。

数据库已有部分列不代表功能已完成，因为管理 API 无法写入这些列。

### P1-07 首页“内容来源”配置不生效

后台允许选择“手动推荐 + 热度排序 / 按更新时间 / 按收入”：[`pages/admin/operations.vue:146`](./pages/admin/operations.vue#L146)。但首页接口只按 `itemIds` 原顺序截取：[`server/api/home.get.ts:12`](./server/api/home.get.ts#L12)，完全不读取 `source` 字段。

运营人员保存自动排序选项后，线上结果不会按所选策略变化，属于静默错误。

### P1-08 管理分析接口没有 RBAC 策略，登录后仍返回 403

- 分析端点存在：[`server/api/admin/analytics.get.ts:6`](./server/api/admin/analytics.get.ts#L6)。
- API 客户端也暴露调用方法：[`composables/useAdminApi.ts:12`](./composables/useAdminApi.ts#L12)。
- RBAC 映射没有 `/api/admin/analytics`：[`shared/admin-rbac.ts:56`](./shared/admin-rbac.ts#L56)。
- 中间件对没有策略的后台接口直接拒绝：[`server/middleware/admin-auth.ts:27`](./server/middleware/admin-auth.ts#L27)。

结果是该接口对所有管理员角色均不可访问，需求中的转化和播放质量指标无法从该端点读取。

### P1-09 可视化/点击验收脚本与当前认证协议失配

[`scripts/visual-check.mjs:41`](./scripts/visual-check.mjs#L41) 仍直接向登录接口提交明文密码；当前接口要求 challenge/proof，因此脚本收到 HTTP 400 并在首个业务页面前终止。注册分支同样没有生成 `passwordSalt/passwordHash`。

这使 README 声明的 `npm run visual-check` 验收命令不可用，现有截图也不能证明当前提交仍通过点击回归。

### P1-10 账号删除由多条独立 SQL 组成，失败时可能部分删除

[`server/api/me/account.delete.ts:15`](./server/api/me/account.delete.ts#L15) 到 [`server/api/me/account.delete.ts:31`](./server/api/me/account.delete.ts#L31) 依次删除观看/分析数据、撤销权益、匿名化订单和用户。中途失败只把隐私请求标记为失败，没有事务或可恢复步骤状态。

影响：可能出现用户仍可登录但权益已撤销、账号已禁用但部分个人数据残留等不一致状态。应使用 D1 batch/事务语义或可重入状态机，并提供后台重试和审计。

### P1-11 当前验收库没有交易闭环数据

`npm run check:acceptance-data` 当前结果：`paid_count=0`、`refunded_count=0`、`revoked_count=0`、`history_count=0`。虽然内容目录已有 5 部剧、7 集和 6 个首页分区，但无法据此验收支付后持久权益、退款回收、历史恢复和后台对账展示。

应在隔离的 Sandbox 验收环境准备至少一组真实 PayPal Sandbox 成功/取消/失败/退款/重复 Webhook 样本；不能用生产库伪造交易。

## 5. P2：一般功能与工程质量问题

### P2-01 播放器存在无功能控件

播放器 CC 和 More 按钮没有点击处理：[`pages/watch/[slug]/[episode].vue:581`](./pages/watch/[slug]/[episode].vue#L581)、[`pages/watch/[slug]/[episode].vue:594`](./pages/watch/[slug]/[episode].vue#L594)。数据库虽允许 `subtitle` 媒体类型，但当前上传/播放流程没有字幕轨选择。

### P2-02 首页展示固定虚构实时人数

[`pages/index.vue:100`](./pages/index.vue#L100) 固定展示 `2,840 viewers watching`，没有数据来源。正式环境不应把静态样例包装成实时运营数据；应接真实指标或删除该模块。

### P2-03 MVP 语言范围发生漂移且翻译不完整

需求 3.2 规定 MVP 仅 `en-US`，其他语言只预留架构；项目却在用户设置中开放西/葡/法/德语，同时大量页面业务文案仍硬编码英文。见 [`composables/useLocale.ts:4`](./composables/useLocale.ts#L4) 与 [`pages/explore.vue:55`](./pages/explore.vue#L55)。

应选择一种口径：MVP 隐藏语言切换并只验收英语，或把完整多语言覆盖正式纳入范围和测试。

### P2-04 自动化测试覆盖不足

仓库只有 3 个测试文件，主要覆盖媒体 Worker 幂等、权益触发器和 PayPal 状态映射。缺少：

- 用户/管理员认证与密码重置安全测试。
- API 权限矩阵、越权、频控和 CSRF/Origin 测试。
- 下单、Capture、Webhook、退款、恢复购买的 API 集成测试。
- 内容 CRUD、定时发布、地区过滤和首页配置策略测试。
- iOS Safari、Android Chrome、弱网、断网恢复和 320 px 视觉回归。

## 6. 需求文档自身的缺陷

### RQ-01 关键业务规则仍是“待确认”，却同时作为 MVP 验收项

需求 18 章明确说这些事项“进入开发前必须确认”，但价格/折扣、未来更新权益、版权国家、退款时限、商户主体等仍未落定：[`海外短剧H5点播平台需求规格说明书.md:526`](./海外短剧H5点播平台需求规格说明书.md#L526)。它们又在支付、权益、地区限制和验收章节被要求实现。

建议将每项改成“已确认值 + 决策人 + 日期 + 变更单号”，未确认项不得作为可验收需求。

### RQ-02 账号与凭据生命周期缺失

文档只列注册、登录、登出和会话 API：[`海外短剧H5点播平台需求规格说明书.md:413`](./海外短剧H5点播平台需求规格说明书.md#L413)，没有定义邮箱验证、忘记密码、重置令牌、会话撤销、密码策略、失败锁定、管理员 MFA 和初始管理员创建流程。

这直接导致当前密码重置实现没有安全验收标准。

### RQ-03 “恢复购买”的身份校验规则不完整且表述冲突

需求 5.4 同时允许订单邮箱、订单号或 PayPal 交易号恢复，又要求二次校验；5.6 则只写邮箱或订单号，没有定义验证因子、账号合并、邮箱不一致、匿名旧订单、退款订单及客服兜底流程。

应给出明确状态机和防枚举响应，不应让开发自行决定“知道订单号是否等于拥有订单”。

### RQ-04 地区版权模型不足以支撑“可见/可售/可播放”

需求只给剧集一个 `target_region/country` 概念，却要求按国家配置版权、不可售区域和下架策略。单值字段无法表达允许列表、禁止列表、生效时间、购买后版权变化、旅行用户和 VPN/未知地区策略。

建议增加 `series_region_policy`，分别定义 discoverable、purchasable、playable、valid_from/to 和已购用户处置规则。

### RQ-05 性能与安全验收不可重复测量

“4G P75 < 2.5 秒”“首帧 P75 < 5 秒”没有规定设备、浏览器、美国测试区域、网络整形参数、冷/热缓存、样本量和统计窗口；“访问频控”“基础防盗播”没有阈值、并发数、令牌复用判定和告警标准。

因此即使实现完成，不同测试人员也可能得到相反结论。应补充可执行的性能测试配置和安全用例表。

### RQ-06 API、数据模型与实现命名不一致

- 文档恢复购买为 `POST /api/orders/{orderNo}/restore`，当前实现为 `POST /api/orders/restore`。
- 文档后台为 `/admin/api/...`，当前实现为 `/api/admin/...`。
- 文档错误字段为 `request_id`，当前类型为 `requestId`：[`types/content.ts:131`](./types/content.ts#L131)。
- 文档 `entitlement/watch_history` 使用 `owner_id/episode_id`，当前数据库使用 `user_id/episode_no`。

API 章节虽然标注“建议”，但文档开头又声明其为单一需求来源。应确定规范性级别，并生成一份与代码一致的 OpenAPI/Schema 作为接口验收基线。

### RQ-07 MVP 范围存在冲突

7.2 要求活动管理和折扣码，15.2 要求活动价；17.4 又把折扣码列为运营增强。3.2 规定 MVP 仅英语，但用户中心又要求语言设置。域名管理要求后台动态绑定备用域名，而当前项目资料把 Cloudflare for SaaS 动态域名作为后续能力。

建议建立明确的 `Must / Should / Later / Out` 范围表，验收只引用 `Must`。

### RQ-08 合规要求只有功能名称，没有法务口径

隐私政策、服务条款、退款政策、版权投诉、年龄分级被列为入口要求，但没有经营主体、适用法律、数据保留期限、Cookie/分析同意、未成年人规则、退款窗口和版权投诉处理信息。对美国市场上线而言，这些不能仅由开发文案代替法务确认。

## 7. 建议修复顺序

1. **立即止血**：关闭无令牌密码重置；为生产密钥和超级管理员加入 fail-fast；升级 Nuxt/Element Plus 并复测。
2. **打通生产支付**：配置 PayPal Production，在隔离环境完成真实 Sandbox 全状态闭环，再验证生产小额订单和 Webhook。
3. **补齐安全边界**：登录/注册/重置/播放频控，播放并发与令牌复用监控，地区可见/可售/可播校验。
4. **修复直接功能错误**：恢复购买输入、探索分区与排序、首页来源策略、analytics RBAC、无效播放器控件。
5. **恢复验收能力**：更新 `visual-check.mjs` 的用户 challenge/proof/注册协议，增加认证、权限、支付和地区集成测试。
6. **补齐后台 MVP 字段**：封面/横幅、标签、演员/版权/SEO、活动价、未来更新策略、定时发布和全局政策配置。
7. **冻结需求 V1.1**：关闭第 18 章未决项，统一 API/字段/范围，补充量化的性能、安全和合规验收表。

## 8. 已具备且本次确认正常的能力

- Nuxt 类型检查和生产构建成功。
- 支付状态映射、权益幂等发放/退款回收、媒体上传幂等测试通过。
- D1 已有 5 部验收短剧、7 集内容、5 部版权数据和 6 个首页分区。
- 主站、后台、媒体域名 DNS/TLS 正常，`www` 301 正确保留路径和查询参数。
- 后台具备管理员账号、角色权限、订单/退款、内容、分类标签、首页分区、域名和审计基础页面。
- 媒体原片走私有 R2，播放地址在服务端完成登录和权益校验后签发。

这些能力说明项目不是“仅 UI 原型”，但不能抵消第 3、4 节中的上线阻塞和核心验收缺口。

## 9. 审计边界

- 本次为代码静态审查、本地构建/测试和已有脚本的只读外部检查。
- 未获得 PayPal Production 凭据，因此没有执行生产支付或退款。
- 没有执行会修改远程 D1、R2、Stream、Cloudflare DNS/规则的命令。
- `visual-check` 在认证阶段失败，因此本次没有用新截图确认所有页面在各 viewport 下的最终视觉状态。
- 依赖漏洞数量以 2026-08-25 当日 `npm audit` 数据为准，升级后需重新审计。
