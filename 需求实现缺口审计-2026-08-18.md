# 海外短剧 H5 点播平台需求实现缺口审计

> 对照文档：`海外短剧H5点播平台需求规格说明书.md`（V1.0 MVP）  
> 审计日期：2026-08-18  
> 审计范围：Nuxt H5、管理后台、服务端 API、Cloudflare D1/R2/Stream、PayPal、迁移、脚本和当前本地配置  
> 审计方法：逐项静态代码核对、类型检查、生产构建、浏览器验收脚本、Cloudflare API 检查及远程 D1 只读查询

## 1. 结论

当前项目已经不再是单纯的 UI 原型，用户认证、真实 HLS 播放器、观看进度、D1 内容模型、R2 分片上传、Cloudflare Stream、PayPal 沙箱支付、退款、后台 RBAC、域名自动化和账号隐私接口均已有代码基础。

但按需求文档的 MVP 上线标准，当前仍不能验收上线。最主要的原因不是页面数量，而是以下四类问题尚未闭环：

1. 存在可直接导致账号接管的认证安全问题。
2. 远程 D1 没有任何真实短剧、分集或可播放媒体，前台仍依赖 mock 内容。
3. 生产支付、Stream Webhook、自定义域名和完整数据库迁移尚未就绪。
4. 内容运营字段、活动价格、搜索筛选、埋点、安全限流、自动化测试和生产运维仍明显不足。

因此当前状态更适合定义为：**主链路代码已搭建、沙箱和演示能力基本具备，但生产安全、真实内容和上线验收未完成。**

## 2. 本次实测结果

### 2.1 代码与构建

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| `npm run typecheck` | 通过 | Vue/Nuxt TypeScript 检查无报错 |
| `npm run build` | 通过 | Node Server 生产构建成功；有一条海报路径运行时解析警告 |
| `npm run check:media-worker` | 通过 | 媒体 Worker 语法检查通过 |
| `npm run check:cloudflare` | 部分通过 | Cloudflare Token 和 Stream API 可访问；域名和 Stream Webhook 配置缺失 |
| `npm run visual-check` | 已执行 | H5、后台页面和鉴权交互均跑到末尾；该脚本只输出截图/控制台 JSON，不构成业务 E2E 测试集 |

### 2.2 远程 D1 当前数据

2026-08-18 只读查询结果：

| 数据 | 数量 |
| --- | ---: |
| 短剧 `series` | 0 |
| 已上架短剧 | 0 |
| 分集 `episodes` | 0 |
| 可播放媒体 `media_assets(status=ready)` | 0 |
| 用户 | 6 |
| 订单 | 2 |
| 已支付订单 | 0 |
| 有效权益 | 0 |

这意味着当前远程环境没有一部可完成“试看 -> 支付 -> 解锁 -> 播放”的真实短剧。

### 2.3 当前外部配置

- PayPal 沙箱连接正常，后台判断为 ready，但生产 PayPal Client ID、Secret、Webhook ID 均未配置。
- PayPal Webhook 尚无任何成功接收时间记录。
- Cloudflare Stream API 和媒体 Worker 已配置，但 `CLOUDFLARE_STREAM_WEBHOOK_SECRET` 为空。
- 自定义域名的 Zone ID 和 CNAME Target 为空，尚未形成真实域名切换能力。
- `REELNOVA_PUBLIC_MOCK_FALLBACK=true`，远程 D1 无正式内容时会展示 mock 短剧。
- `CLOUDFLARE_ACCESS_REQUIRED=false`，后台未启用 Cloudflare Access 前置保护。
- `.env` 未覆盖 `SUPER_ADMIN_PASSWORD`、`ADMIN_SESSION_SECRET`、`USER_SESSION_SECRET`。

## 3. P0：上线阻塞和严重安全问题

### P0-01 密码重置可被用于接管任意用户账号

`POST /api/auth/password-reset` 只接收邮箱和新密码，找到账号后直接更新密码，没有邮件验证码、一次性令牌、旧密码或已登录校验。攻击者只要知道用户邮箱即可修改其密码。

**证据：** `server/api/auth/password-reset.post.ts:4-14`、`server/utils/user-auth.ts:132-147`。

**完成标准：** 改为“申请重置 -> 邮件一次性令牌 -> 校验令牌 -> 修改密码”；令牌需哈希存储、短时有效、单次使用，并对申请和验证接口做频控。响应不得泄露邮箱是否注册。

### P0-02 默认超级管理员和会话密钥仍在实际使用

运行配置在未提供 Secret 时回退到公开在仓库中的默认管理员密码和固定会话密钥。本次实测使用 `admin@reelnova.com / ReelNova@2026` 可成功登录当前后台；同时 Cloudflare Access 被显式关闭。已知会话密钥还会带来伪造用户或管理员 Cookie 的风险。

**证据：** `nuxt.config.ts:55-60`、`server/utils/admin-auth.ts:119-160`；当前 `.env` 未定义三项生产 Secret。

**完成标准：** 生产启动时强制校验随机高强度的超级管理员初始密码、管理员会话密钥和用户会话密钥，缺失即拒绝启动；立即轮换当前超级管理员密码和所有会话；生产启用 Cloudflare Access；增加登录限流、失败锁定和安全告警。

### P0-03 远程数据库迁移状态与代码不一致

代码已经依赖 `admin_accounts.permission_role`，但远程 D1 的 `admin_accounts` 表没有该列，说明 `0015_admin_rbac.sql` 未应用；同时 `0016_paypal_environment.sql` 已应用。当前健康检查只验证 0016，因而错误地报告数据库正常。

影响包括：新增普通管理员、切换内容/财务角色会直接 SQL 失败；普通管理员权限可能按默认值误判。

**证据：** `migrations/0015_admin_rbac.sql:5-9`、`server/utils/admin-auth.ts:104-110`、`server/utils/admin-auth.ts:258-264`、`server/api/admin/connection.get.ts:31-44`；远程 `pragma_table_info('admin_accounts')` 无 `permission_role`。

**完成标准：** 建立迁移版本表和严格的顺序化部署；补应用 0015（需先验证 D1 对现有表执行该 `ALTER TABLE` 的兼容性）；健康检查核对所有必需表、列、索引和触发器，而不是只检查一个最新字段。

### P0-04 mock 内容与真实下单入口未隔离

当 D1 没有已上架短剧时，公共内容接口回退到 `data/mock.ts`。订单接口也通过同一个公共内容列表查找短剧，并在数据库查不到真实价格记录时继续使用 mock 价格生成订单。因此一旦切换到生产 PayPal，可能对没有真实分集和媒体的样例短剧收费。

**证据：** `server/utils/managed-content.ts:175-183`、`server/api/orders/index.post.ts:96-101`、`server/api/orders/index.post.ts:158-181`。

**完成标准：** 生产环境强制关闭 mock fallback；创建订单只能查询 D1 中 `published`、未删除、地区可售且至少存在可播放分集的真实短剧；价格必须来自已发布价格版本，不允许回退到展示层对象。

### P0-05 真实内容和媒体数据为空

虽然上传、转码和播放代码已存在，但远程 D1 中短剧、分集和 ready 媒体均为 0，无法完成真实播放验收。当前“可看到内容”仅代表 mock 页面可用，不代表内容生产链路已经通过。

**完成标准：** 至少导入 1 部正式测试短剧，完成封面、分集、R2 原片、Stream 转码、Webhook 回写、免费集播放、付费集拦截和后台预览的完整验收。

### P0-06 生产支付与媒体回调未就绪

当前只有 PayPal 沙箱可用，生产凭据为空；Stream Webhook Secret 为空，转码完成回调会返回 503。后台 `mediaConfigured` 仍可能显示媒体链路可用，因为该判断没有要求 Stream Webhook 已配置。

**证据：** `server/api/media/stream-webhook.post.ts:17-26`、`server/api/admin/connection.get.ts:76-89`。

**完成标准：** 配置并验证 PayPal 生产 Client ID/Secret/Webhook；配置 Stream Webhook Secret 和回调 URL；后台 readiness 必须把回调、媒体 Worker、R2、Stream token 和真实探测全部纳入检查。

### P0-07 媒体上传完成流程缺少可恢复幂等状态机

上传完成接口先将会话设为 `completing`，再调用 Worker 完成 R2 并创建 Stream Copy，随后分多次写 D1。如果 Worker 已创建 Stream 视频但响应或 D1 更新失败，会话停在 `completing`，当前接口又不允许该状态重试。Worker 对已存在的 R2 对象仍会再次创建 Stream Copy，可能产生重复视频和孤立资源。

创建上传会话时也先创建 R2 multipart，再写 D1；D1 写入失败会留下无法管理的 multipart upload。

**证据：** `server/api/admin/media/uploads/[uploadId]/complete.post.ts:24-69`、`workers/media-worker.mjs:123-140`、`server/api/admin/series/[id]/episodes/uploads.post.ts:51-86`。

**完成标准：** 为上传会话、R2 完成和 Stream Copy 使用稳定幂等键；`completing` 可查询并恢复；保存外部资源 ID 后再推进状态；增加孤立 multipart、R2 对象和 Stream 视频的对账清理任务。

## 4. P1：需求功能仍不完整

### 4.1 用户端 H5

| ID | 模块 | 缺失或仅部分实现 | 代码现状 |
| --- | --- | --- | --- |
| P1-01 | 首页 | 下拉刷新、分页/分区懒加载、返回滚动位置、真实 Tab 切换 | Tab 只滚到固定分区，Rankings 复用 Popular，Categories 固定指向 Romance；没有分页和滚动恢复。`pages/index.vue:7-12` |
| P1-02 | 首页运营策略 | 按更新时间/收入自动选片未生效 | 后台可选 `source`，但首页 API 始终只按 `itemIds` 取内容。`server/api/home.get.ts:12-17` |
| P1-03 | 探索页 | 类型、语言、标签筛选及四种真实排序 | 服务端只支持关键词；类型和排序在客户端对一次性结果处理；Most Purchased 实际按“当前用户是否已购”排序。`server/api/explore.get.ts:5-12`、`pages/explore.vue:17-26` |
| P1-04 | 详情页 | 分享、相关推荐、导演/版权/字幕等资料 | 分享按钮没有事件；没有推荐短剧区域；资料只显示分类、简介和演员。`pages/series/[slug].vue:47-70` |
| P1-05 | 购买说明 | 后续更新权益策略和政策链接可配置 | 页面硬编码“包含 future updates”；Terms/Refund Policy 只是文本，不是可配置链接。`pages/series/[slug].vue:69`、`components/UnlockSheet.vue:169` |
| P1-06 | 播放器 | 字幕轨道、字幕开关、手动清晰度选择 | CC 按钮无行为；没有 `<track>` 数据和清晰度菜单。HLS 自适应存在，但未达到需求中的可选择状态。`pages/watch/[slug]/[episode].vue:196` |
| P1-07 | 用户中心 | 独立支付记录、完整退款详情、真实政策页 | 已有订单/已购/历史，但没有独立支付流水视图；政策和客服邮箱硬编码。 |
| P1-08 | 国际化 | 所有用户文案由翻译资源配置 | `useLocale` 只覆盖部分 Profile 文案，首页、详情、播放器、支付和认证仍大量硬编码英文。 |

### 4.2 内容和运营后台

| ID | 模块 | 缺失或仅部分实现 | 代码现状 |
| --- | --- | --- | --- |
| P1-09 | 短剧资料 | 英文名/别名、短标题、导演、版权、语言、字幕、SEO、分享图、解锁策略 | 数据表预留部分字段，但后台表单/API 只维护标题、简介、分类、地区、试看数和单一价格。 |
| P1-10 | 图片素材 | 主封面、列表封面、横幅、缩略图上传与裁切 | 新建短剧固定使用示例海报；后台没有图片上传入口。`server/utils/normalized-content.ts` 的创建逻辑写死 `/posters/vows-vengeance*`。 |
| P1-11 | 标签联动 | 给短剧选择多个标签并按启用状态/有效期展示 | 标签表和管理页存在，但短剧编辑页没有标签字段；前台主要取首个标签或旧 badge。 |
| P1-12 | 批量运营 | 批量改分类、标签、试看数和价格 | 当前批量操作只有上架、下架和版权冻结。`pages/admin/series.vue` |
| P1-13 | 定时发布 | 定时上架/下架调度和发布预览 | 数据表有 `scheduled_at` 和版本表，但编辑表单没有时间字段，仓库中没有 Cron/队列调度任务；状态切换立即生效。 |
| P1-14 | 活动价格 | 活动价、地区价、有效期、折扣码、次数上限 | 仅支持一个 USD 价格；订单虽保存价格快照，但没有活动配置和使用计数模型。 |
| P1-15 | 推荐位 | 权重、开始/结束时间 | 只有首页分区和手工 `itemIds`，没有推荐权重和生效时段。 |
| P1-16 | 分类素材 | 分类图标/封面、多语言资源 | 数据表预留 `icon_url/cover_url`，后台管理未开放这些字段。 |
| P1-17 | 全局站点配置 | 站点名、Logo、客服邮箱、服务条款、隐私政策、退款政策 | 没有 site-config API/后台页面，ReelNova、Logo、邮箱和政策文案散落在组件及 PayPal 请求中。 |

### 4.3 支付、数据和 API

| ID | 模块 | 缺失或风险 | 代码现状 |
| --- | --- | --- | --- |
| P1-18 | 支付一致性 | capture、订单、权益和用户资料缺少原子批处理 | 已改为“先幂等发权益、再更新订单”，重试可补偿，较旧版本安全；但仍是多次独立 D1 写入，缺少事务/批处理和自动对账。`server/utils/paypal.ts:311-349` |
| P1-19 | 自动补偿 | pending 支付、失败 Webhook、退款和缺失权益的定时补偿 | 有后台手工核验/重试，但没有 Cron 自动查询、重放和告警任务。 |
| P1-20 | 统一错误格式 | 所有错误返回 `code/message/request_id` | 只有成功响应统一为 Envelope；大量 `createError` 仍走 Nuxt/H3 默认错误格式。`server/utils/response.ts:1-8` |
| P1-21 | 审计结构 | 高风险操作的 before/after JSON | 审计表只有 `detail` 文本，没有 `before_json/after_json`。`migrations/0004_admin_audit_logs.sql:3-13` |
| P1-22 | 域名发布检查 | PayPal 回调/白名单、旧域名迁移检查清单 | Custom Hostnames 和 301 代码已存在，但当前无真实 Zone/CNAME；也没有支付回调发布检查流程。 |

## 5. P2：安全、性能、测试和运维缺口

| ID | 模块 | 缺口 | 影响 |
| --- | --- | --- | --- |
| P2-01 | 防滥用 | 登录/注册/重置/播放接口频控、设备并发限制、试看频次限制、异常下载识别 | 容易被撞库、刷试看、批量请求播放 token；不满足需求 5.3、9.3、13、16。 |
| P2-02 | 播放令牌 | Stream token 绑定用户会话/IP/设备 | 当前 Cloudflare Stream token 只设置过期时间；业务 tracking token 绑定会话，但媒体 token 本身未绑定。`server/utils/media-pipeline.ts:165-169` |
| P2-03 | 埋点 | 首页/卡片曝光点击、详情、锁片、支付、搜索筛选分享、首帧/卡顿/失败 | 当前主要只有 start/heartbeat/complete 播放事件，无法计算需求第 14 章大部分指标。 |
| P2-04 | 图片性能 | WebP/AVIF、响应式尺寸、首图优先加载 | 海报主要为固定 JPG，没有图片变体服务和明确 LCP 优化。 |
| P2-05 | 自动化测试 | 单元、API 集成、权限、支付幂等、安全和完整 E2E | `package.json` 没有 test 脚本，仓库没有测试用例；`visual-check` 主要验证截图、溢出和少量交互。 |
| P2-06 | 兼容/网络 | iOS Safari、Android Chrome、内嵌浏览器、4G/弱网/断网、美国节点 | 当前只在桌面 Chrome 内核执行视觉检查，没有目标设备和网络验收证据。 |
| P2-07 | 性能指标 | 首页 P75、播放首帧 P75、卡顿率、地区/设备/CDN 维度 | 没有 RUM/性能采集、阈值告警或负载测试结果。 |
| P2-08 | 生产运维 | 日志聚合、告警、备份恢复、发布回滚、资源对账 | 仓库只有状态页面和手工操作，缺少定时任务、告警规则、备份演练和回滚方案。 |
| P2-09 | 合规内容 | 正式隐私政策、服务条款、退款政策、版权投诉和年龄提示 | 当前多为页面内硬编码摘要/邮箱，未形成可配置、可发布、可追踪版本的正式政策。 |

## 6. 已具备的能力

以下能力已有可见实现，不应再按“完全缺失”处理：

- 用户注册、登录、退出、全局前台路由拦截和账号级会话。
- H5 首页、探索、详情、播放器、片库、Profile 和订单/历史视图。
- 原生 HLS/hls.js 播放、进度、倍速、全屏、下一集、断线重试、签名续期和跨设备观看进度。
- 服务端试看/权益校验、Origin/Referer 基础校验和短时 Cloudflare Stream token。
- D1 规范化短剧、分集、分类、标签、媒体、上传会话、转码任务和内容版本模型。
- MP4/MOV 前端元数据检查、R2 multipart 断点续传、Stream Copy、进度、预览和手工重试。
- PayPal 官方按钮、创建订单、capture、Webhook 验签、幂等订单、价格快照、风险金额校验。
- 退款 API、退款 Webhook、权益回收、后台核验和失败 Webhook 手工重试。
- 用户订单恢复、已购片库、观看历史、数据导出和账号删除。
- 内容运营、财务运营、超级管理员三类 RBAC 的代码定义及路由/API 权限映射。
- 首页分区增删改、显示隐藏和拖拽排序。
- 用户查询、禁用/恢复、设备限制解除和手工补发权益。
- PayPal 环境切换、自定义域名/证书/301 的代码实现。

## 7. MVP 验收判断

| 需求验收项 | 当前判断 | 主要原因 |
| --- | --- | --- |
| 登录后浏览首页、分类、详情 | 部分通过 | 路由和页面可用；真实分类/排序、首页策略和正式内容未完成 |
| 按配置播放试看集 | 代码具备，环境不通过 | 播放器和校验存在，但远程无真实分集/媒体，Stream Webhook未配置 |
| 锁片后 PayPal 支付并解锁 | 沙箱部分通过 | 沙箱连接 ready；生产支付未配置，真实内容为空，缺少完整 E2E 证据 |
| 刷新后权益仍存在 | 代码具备 | D1 权益和恢复逻辑存在；尚无真实已支付订单验收 |
| 用户查看订单、已购和观看记录 | 基本具备 | 核心 API/页面存在；独立支付流水和完整退款展示仍不足 |
| 后台上传、转码、预览和上架 | 代码部分通过 | 主链路已实现；无真实数据验收，Webhook/幂等恢复未闭环 |
| 后台维护完整资料、价格、活动、分类标签 | 不通过 | 只覆盖基础字段和单价，素材、标签联动、活动、定时发布缺失 |
| 后台订单、退款、对账 | 基本具备 | 查询、CSV、退款和对账存在；自动补偿、生产 PayPal 未完成 |
| 域名切换并保持数据 | 代码具备，环境不通过 | Zone/CNAME 为空，无真实域名和证书验收 |
| 短时签名、防盗播、限流 | 部分通过 | 有签名和基础同源校验；缺频控、并发设备和异常下载识别 |
| 高风险操作完整审计 | 部分通过 | 有操作人/时间/IP/摘要；缺结构化前后值，部分动作无完整审计 |
| 320 px 和多浏览器兼容 | 未充分证明 | 有 390 px Chrome 截图，缺 320 px、Safari、Android 和内嵌浏览器验收 |

## 8. 建议实施顺序

1. **先封堵认证风险：** 下线当前密码重置、轮换管理员密码和会话 Secret、开启 Cloudflare Access、加登录/重置频控。
2. **修复环境一致性：** 补齐 0015 迁移，建立迁移版本检查，关闭生产 mock fallback，修正 readiness 假阳性。
3. **导入真实内容跑通媒体：** 配置 Stream Webhook，导入至少一部测试剧，完成上传、转码、回调、预览、试看和锁片播放。
4. **完成生产支付验收：** 配置 PayPal production，使用真实内容执行成功/失败/取消/重复回调/退款/恢复购买全场景测试。
5. **补内容运营能力：** 完整资料和素材、标签联动、活动/地区价格、定时发布、推荐权重和全局站点配置。
6. **补前台发现和合规：** 服务端筛选排序、首页自动策略、分享/推荐、字幕/清晰度、正式政策和可配置客服信息。
7. **补安全、指标和运维：** 接口限流、并发设备、埋点/RUM、自动补偿、日志告警、备份回滚和资源对账。
8. **建立自动化验收：** 单元/API/E2E/安全测试，并完成 iOS、Android、内嵌浏览器、弱网和美国节点测试。

## 9. 评估边界

- 本报告以仓库代码、当前 `.env` 配置状态和 2026-08-18 远程 D1 实测为准；后续外部平台配置变化会改变环境类结论。
- 未把需求明确列为非目标的订阅、评论、私信、社交关注等功能计入缺口。
- 收藏入口在需求中允许 MVP 隐藏，因此不计为阻塞项。
- “代码已存在”不等于“生产已验收”；涉及 Cloudflare、PayPal、域名和真实设备的能力均单独标记了环境验证状态。
