# 海外短剧 H5 点播平台缺口与点击测试报告

> 审计日期：2026-08-20  
> 对照文档：`海外短剧H5点播平台需求规格说明书.md`  
> 范围：Nuxt H5、管理后台、服务端 API、D1/R2/Stream、PayPal、响应式、可访问性、构建、依赖与自动化测试  
> 方法：需求对照、代码审查、远程 D1/Cloudflare 健康检查、Chrome/Playwright 真实点击和小屏布局检查

## 1. 总体结论

项目已具备可运行的 MVP 技术主链路：账号会话、D1 规范化内容模型、R2/Stream 媒体处理、真实 HLS 播放、锁片鉴权、PayPal 官方按钮、订单/退款基础接口、后台 RBAC 和 Cloudflare 构建都已有实现，不再是纯 UI 原型。

但当前仍不满足正式上线条件，主要原因是：

1. 存在可直接接管任意用户账号的密码重置漏洞。
2. 当前环境仍使用公开默认管理员凭据和固定会话密钥回退，Cloudflare Access 未启用。
3. Stream Webhook、PayPal 生产凭据、自定义域名 CNAME 尚未配置，生产回调与域名切换无法验收。
4. 线上 D1 只有 1 部上传测试剧、1 个首页分区、0 笔可点击订单，不符合需求中 5-6 个分区及完整购买/退款验收数据的要求。
5. 首页分类、分享、政策链接、字幕/清晰度、活动价格、定时发布、埋点、限流、监控和跨浏览器自动化仍不完整。

**当前定位：主链路开发/沙箱联调阶段，不建议开放正式用户。**

## 2. 本次验证结果

### 2.1 构建、数据库与依赖

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| `npm run typecheck` | 通过 | Vue/Nuxt TypeScript 无报错 |
| `npm run build` | 通过 | Node 生产构建成功；存在海报路径运行时解析警告 |
| `npm run build:cloudflare` | 通过 | Cloudflare Pages 产物生成成功；提示 Node.js compatibility 未开启，且有超过 500 kB 的客户端分块 |
| `npm run db:migrate:check` | 通过 | D1 迁移 `17/17`，表、列、索引、触发器契约完整 |
| `npm run check:media-worker` | 通过 | Worker 语法检查通过 |
| `npm run test:media-worker` | 4/4 通过 | 覆盖上传/完成幂等、Range、Stream token 和 playback grant |
| `npm run check:cloudflare` | 部分通过 | Token、Stream API、Zone API 均返回 200；Stream Webhook Secret 和 CNAME Target 缺失 |
| `npm audit --omit=dev` | 不通过 | 14 个漏洞：2 critical、6 high、3 moderate、3 low；涉及 Nuxt DevTools、tar、glob、unhead、Element Plus 等 |

### 2.2 Cloudflare 和 PayPal 当前状态

| 项目 | 当前状态 |
| --- | --- |
| D1 连接/模式 | 正常，Cloudflare REST API |
| Stream API / 媒体 Worker / 播放签名 | 已配置，实测 HLS 可播放 |
| Stream Webhook | **未配置** `CLOUDFLARE_STREAM_WEBHOOK_SECRET` |
| 媒体整体 readiness | **false**，原因是 Stream Webhook 缺失 |
| PayPal Sandbox | 连接正常，前后端 Client ID 匹配，Webhook ID 已配置 |
| PayPal Production | **未配置** Client ID / Secret / Webhook / Browser Client ID |
| 最近 PayPal Webhook | 无成功接收时间 |
| 自定义域名 | Zone 可访问，但 CNAME Target 缺失，readiness=false |
| Cloudflare Access | **关闭** |

### 2.3 点击测试

| 流程 | 结果 | 实测说明 |
| --- | --- | --- |
| 注册 / 登录 / 登录后返回首页 | 通过 | 新建测试账号后会话可用，但远程 D1 请求较慢，按钮会长时间停留在 `Please wait...` |
| 密码重置 | **严重失败** | 只输入账号邮箱和新密码即返回 200 并改密，无邮件验证码、一次性 token、旧密码或登录态校验 |
| 首页 | 部分通过 | 真实数据显示 1 个分区、1 张卡片；4 个 Tab 均可点，但当前数据只有 Popular，New/Categories 无对应分区，Rankings 复用 Popular |
| Explore 搜索/类型/排序/清除 | 通过基础交互 | 面板、Romance、Most Watched、无结果和清除操作可点；只有 1 条数据，不能证明排序正确性 |
| 真实剧集详情 | 部分通过 | `upload-test-big-buck-bunny` 可进入，3 集状态正常；Share 点击无任何反馈；解锁弹窗可打开，Terms/Refund 可点链接数为 0 |
| 免费集 HLS 播放 | 通过，性能待优化 | 播放授权 200，HLS 清单和分片均 200，进度可继续增长；本地实测点击后约 5-10 秒进入 `readyState=4` |
| 锁定集 | 通过 | 不创建 `<video>`，展示锁片 CTA，点击后 PayPal 官方按钮容器可见 |
| Profile / Library / 订单 / 历史 | 部分通过 | Profile 可打开；新用户无订单/历史，页面进入错误/空数据态，没有可点详情数据 |
| 后台登录和 11 个主要页面 | 通过 | 概览、短剧、订单、首页配置、分类、用户、对账、系统、域名、审计、管理员均可进入，无控制台异常 |
| 后台“新建短剧” | 通过 | 点击后对话框正常打开；未提交，避免污染正式 D1 |
| 后台订单详情 | 未能验证 | 当前列表 0 笔订单，无“详情”按钮 |
| 375 px 竖屏 / 844×390 横屏 / 后台 390 px | 布局基本通过 | 未发现页面级横向溢出；但存在多个 36-40 px 高的可点元素和 8-14 px 文字 |

## 3. P0：上线阻塞项

### P0-01 密码重置可接管任意账号

`server/api/auth/password-reset.post.ts:4-17` 只验证邮箱格式、密码强度和前端生成的哈希格式，随后直接按邮箱更新密码。本次使用测试账号点击实测已成功改密。

**完成标准：**改为“申请重置 -> 邮件一次性 token -> 验证 token -> 改密”；token 哈希存储、短时有效、单次使用；请求和验证接口限流；响应不泄露邮箱是否已注册。

### P0-02 默认管理员和固定会话密钥仍可使用

`nuxt.config.ts:55-61` 在环境变量缺失时回退到公开的 `admin@reelnova.com / ReelNova@2026` 和固定 session secret。当前 `.env` 未设置 `SUPER_ADMIN_PASSWORD`、`ADMIN_SESSION_SECRET`、`ADMIN_CREDENTIAL_SECRET`、`USER_SESSION_SECRET`，且本次仍可用默认凭据登录后台。`CLOUDFLARE_ACCESS_REQUIRED=false`。

**完成标准：**生产启动时强制检查高强度独立 secrets，缺失则拒绝启动；立即轮换当前管理员密码和所有会话；生产开启 Cloudflare Access；增加登录限流、失败锁定和告警。

### P0-03 生产媒体、支付和域名回调未就绪

后台健康接口明确返回：`mediaConfigured=false`、`streamWebhookConfigured=false`、PayPal production 全部未配置、`customHostnamesConfigured=false`。虽然现有 ready 媒体可播放，但新上传的转码回写、正式扣款、正式 Webhook 和域名迁移都无法完成生产验收。

**完成标准：**配置 Stream Webhook Secret/回调 URL、PayPal 生产四项凭据、CNAME Target；分别完成转码回调、生产小额支付/退款、域名证书和 301 验收。

### P0-04 没有可用于 MVP 验收的正式内容和交易数据

当前公开 API 仅返回 `Upload Test - Big Buck Bunny`，首页只有 1 个 `Popular now` 分区和 1 部剧。订单为 0，因此不能验证“支付成功 -> Webhook -> 权益 -> 刷新仍可播放 -> 退款回收”。

**完成标准：**准备至少 5 部合规验收内容和 5-6 个有效分区；至少 1 部完成封面、多集、试看、锁片、支付、权益、历史和退款的全链路验收。生产强制关闭 mock fallback。

### P0-05 安全依赖存在 critical/high 漏洞

`npm audit --omit=dev` 报告 2 个 critical 和 6 个 high，其中包括 Nuxt DevTools 本地 RPC 命令执行、`tar` 任意文件覆盖/读写和 `glob` CLI 命令注入。当前 `devtools.enabled=false` 会降低生产暴露，但不能代替依赖升级和回归验证。

**完成标准：**在分支中升级 Nuxt/Element Plus 及间接依赖，不盲目在生产分支执行 `npm audit fix --force`；重跑类型、Node/Cloudflare 构建、播放、后台和支付沙箱回归，消除 critical/high。

## 4. P1：核心功能与交互不足

| ID | 模块 | 缺失/不足 | 证据与影响 |
| --- | --- | --- | --- |
| P1-01 | 首页发现 | 下拉刷新、分页/分区懒加载、返回滚动位置、真实 Tab 数据切换 | `pages/index.vue:38-42` 将 Rankings 映射到 Popular、Categories 写死为 `romance`；当前数据没有对应 DOM 目标 |
| P1-02 | 首页运营 | 配置的“按更新/收入自动排序”来源策略没有完整贯通 | 页面与 API 仍主要按配置 `itemIds` 取内容；“2,840 viewers watching”为硬编码 |
| P1-03 | Explore | 缺语言、标签筛选；类型写死；排序只在前端对已加载集合进行 | `pages/explore.vue:11-26`；Most Purchased 实际按当前用户 `purchased` 布尔值排序，不是全站销量 |
| P1-04 | 详情页 | Share 无处理、无成功/失败反馈；无相关推荐 | `pages/series/[slug].vue:50` 只渲染按钮。实测点击后 URL、弹窗和状态均未变化 |
| P1-05 | 支付与合规 | 解锁弹窗 Terms/Refund Policy 是普通文本，注册页 Terms/Privacy 也不可点；无公开独立正式政策 URL | 实测弹窗政策链接数为 0；当前 `/profile/terms` 需先登录且只是摘要 |
| P1-06 | 播放器 | Captions 按钮无事件/无 `<track>`，无手动清晰度菜单 | `pages/watch/[slug]/[episode].vue` 仅 HLS 自适应；未达到“字幕和可选清晰度”要求 |
| P1-07 | 播放状态 | 起播期间缺少明确 loading 状态，较慢时容易误以为点击无效 | 本地实测前 5 秒 `readyState=0`，约 5-10 秒后起播；需正式美国 4G P75 测试确认是否达到 5 秒目标 |
| P1-08 | 内容字段 | 后台未完整维护别名、短标题、导演、版权、字幕、SEO、分享图、解锁范围策略 | D1 表已预留多个字段，但短剧表单/API 仍主要维护基础字段 |
| P1-09 | 图片素材 | 缺少主封面/列表封面/横幅/分享图的完整上传与裁切工作流 | 当前主要使用固定 JPG 和海报 API，不满足多素材管理 |
| P1-10 | 发布/批量运营 | 缺定时上下架调度、批量改分类/标签/试看数/价格、推荐权重和生效时段 | `series.scheduled_at` 已建模，但未找到 Cron/队列调度和完整管理交互 |
| P1-11 | 价格与活动 | 缺活动价、地区价、有效期、折扣码、次数上限和用税规则 | 当前主要是单一 USD 价格，不能完成需求 7.2 的活动管理 |
| P1-12 | 全局配置/国际化 | 站点名、Logo、客服邮箱、政策文本仍散落硬编码；翻译只覆盖部分 Profile | 缺独立 site-config API/管理页；首页、详情、播放、认证仍大量硬编码英文 |
| P1-13 | 安全防滥用 | 缺登录/注册/重置/播放的频控、设备并发播放限制、试看频次限制和异常下载识别 | 当前有短时 Stream token 和业务鉴权，但未找到通用 rate limiter/并发会话控制 |
| P1-14 | API/审计 | 错误响应未统一 `code/message/request_id`；审计无结构化 before/after | 成功响应使用 envelope，大量 `createError` 仍是 H3 默认结构；`admin_audit_logs` 只有文本 `detail` |
| P1-15 | 埋点/指标 | 缺曝光、卡片点击、详情、锁片、支付、搜索、分享、首帧、卡顿、失败等埋点 | 现有主要是 start/heartbeat/complete 播放事件，无法计算需求第 14 章多数指标 |

## 5. P2：质量、性能和运维缺口

| ID | 模块 | 缺口/影响 |
| --- | --- | --- |
| P2-01 | 官方视觉验收脚本 | `scripts/visual-check.mjs:39-49` 仍向已升级的 challenge/proof 认证 API 发送明文密码，首个受保护用户页就以 400 退出；且 `:115-117` 写死了已不存在于真实 D1 的 `vows-and-vengeance` |
| P2-02 | 自动化测试 | 仅 1 个 Worker 测试文件；缺认证、RBAC、API、订单、Webhook、退款、播放鉴权、历史和端到端回归；`package.json` 没有统一 `test`/CI 验收命令 |
| P2-03 | 可访问性/触控 | 375 px 实测中 Explore 类型 chip 高 36 px，锁片次要按钮高 40 px，低于需求的 44 px；移动端 body 为 14 px，页面存在大量 8-11 px 功能文字，不利于阅读和系统字体放大 |
| P2-04 | 图片/包体性能 | 海报主要为固定 JPG，无 WebP/AVIF、`srcset/sizes` 和首图优先策略；构建提示客户端 chunk >500 kB，Cloudflare 产物中短剧后台分块约 780 kB |
| P2-05 | 响应时间 | 本地通过 Cloudflare REST 读 D1 时，匿名会话约 1 s、首页 API 约 2.6 s；注册/登录后路由会等待账号设置。尚无 4G P75、美国节点和并发基准 |
| P2-06 | 兼容性/网络 | 本次只有桌面 Chrome 内核的竖屏/横屏模拟；未覆盖 iOS Safari、Android Chrome、常见内嵌浏览器、4G/弱网/断网恢复和美国节点 |
| P2-07 | 生产运维 | 缺日志聚合、告警、备份恢复演练、发布回滚、支付/Webhook 自动补偿、孤立媒体对账和 SLO/RUM 监控证据 |
| P2-08 | Cloudflare 构建配置 | 构建警告 Node.js compatibility 未开启；应根据实际 Worker 使用的 Node API 确认 `nodejs_compat` 标志和 compatibility date，并用 `wrangler pages dev` 回归 |

## 6. 需求验收影响

| MVP 验收项 | 当前判断 | 主要原因 |
| --- | --- | --- |
| 注册/登录后浏览 | 部分通过 | 流程可用，但密码重置可接管账号，且请求较慢 |
| 首页 5-6 个分区和内容发现 | 不通过 | 当前仅 1 个分区、1 部测试剧，Tab 不是真实切换 |
| 按配置播放试看集 | 代码/本地链路通过 | 签名 HLS 真实可播；起播性能、字幕/清晰度和目标设备待验收 |
| 锁片后 PayPal 支付并解锁 | 部分通过 | 锁片与官方按钮可打开；未执行完整 Sandbox capture/Webhook/权益验收 |
| 刷新后权益仍存在 | 未能验证 | 无已支付测试订单/权益 |
| 用户查看订单、已购、历史 | 界面通过，数据未验证 | 新测试用户数据为空 |
| 后台上传、转码、预览和上架 | 部分通过 | 已有 ready 测试媒体和 Worker 测试；Stream Webhook 未配置，本次未新建外部资源 |
| 完整内容、价格、活动、分类标签管理 | 不通过 | 字段和批量/调度/活动工作流未完整 |
| 订单、退款、对账 | 代码部分通过 | 接口与页面存在，但当前 0 订单且无生产 PayPal |
| 生产域名切换 | 不通过 | CNAME Target 缺失，未完成证书/301/回调白名单验收 |
| 安全、限流、审计 | 不通过 | 存在账号接管、默认 secrets、无限流、审计前后值缺失 |

## 7. 建议修复顺序

1. **立即封堵账号接管：**下线现有无验证重置接口，实现邮件 token 流程，清理现有会话并审查近期重置日志。
2. **完成生产安全基线：**轮换默认管理员和四项 secrets，开启 Cloudflare Access，增加认证/播放限流，升级有 critical/high 漏洞的依赖。
3. **补齐外部回调：**先配 Stream Webhook，再完成 PayPal Production 和自定义域名，将健康检查全部变为 ready。
4. **建立可重复的验收数据：**导入 5+部测试内容、首页 5-6 分区、可支付测试订单，使用固定 seed/fixture，不再把 slug 和账号写死在测试脚本。
5. **修复自动化回归：**升级 `visual-check` 到 challenge/proof 认证，增加 API/Playwright 端到端用例，在 CI 中覆盖认证、试看、锁片、支付幂等、Webhook、退款、RBAC 和响应式。
6. **完成用户端和运营端缺口：**优先补分享/政策链接、首页/Explore、字幕/清晰度，再补内容字段、活动价格、定时发布和全局配置。
7. **最后做生产级验收：**真实 iOS/Android/内嵌浏览器、美国 4G/弱网、首帧 P75、并发播放、备份恢复、告警和回滚演练。

## 8. 已具备能力（不应再列为“完全缺失”）

- D1 规范化的短剧、分集、分类、标签、媒体、上传会话、转码任务、订单、权益和观看历史表，迁移 17/17 健康。
- 注册、challenge/proof 登录、Cookie 会话、用户/后台路由保护。
- R2 multipart、Stream Copy、上传幂等、转码状态、预览和重试的代码基础。
- 真实 `<video>` + HLS/hls.js、签名播放地址、播放/暂停、拖动、音量、倍速、全屏、下一集、签名续期和观看进度。
- PayPal 官方按钮、创建/capture、Webhook 验签、价格快照、幂等权益、退款和失败 Webhook 手工重试的代码基础。
- 内容运营、财务运营、超级管理员 RBAC，以及后台用户、订单、对账、域名和审计界面。
- 375 px 竖屏、844×390 横屏和后台 390 px 没有页面级横向溢出，并已有 `prefers-reduced-motion` 基础处理。

## 9. 本次测试边界

- 未执行真实 PayPal 扣款、capture、退款和生产 Webhook，因为当前只配置 Sandbox 且测试账号没有已支付订单。
- 未上传新的大视频，避免创建额外 R2/Stream 资源；已验证现有 ready 媒体可播放和 Worker 幂等测试。
- 未执行删除账号、退款、变更管理员和变更生产域名等破坏性操作。
- 布局测试使用 Chrome 视口模拟，不等价于真机 Safari/Chrome 和美国网络验收。
