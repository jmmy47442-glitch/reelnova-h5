# 海外短剧 H5 点播平台功能缺口分析

> 对照文档：`海外短剧H5点播平台需求规格说明书.md`（V1.0 MVP）  
> 评估日期：2026-08-11  
> 评估范围：当前仓库中的 Nuxt 页面、组件、服务端 API、Cloudflare D1 迁移和自动化脚本  
> 评估方式：静态代码逐项对照，并执行 `npm run typecheck`  

## 1. 结论

当前项目已经具备较完整的用户端和管理端界面、用户/管理员登录、D1 订单与权益基础表、PayPal 创建订单与 Webhook 验签等基础能力，但整体仍属于“高保真 UI + 部分业务 API”的阶段，尚未形成需求文档要求的可上线 MVP 闭环。

### 1.1 实施进度（2026-08-11）

本报告生成后已完成第一批主链路补齐：

- `P0-01` 已接入真实 `<video>`、原生 HLS/hls.js、签名 URL、进度拖动、音量、倍速、全屏、下一集、断网恢复和签名续期；字幕轨道和手动清晰度选择仍依赖媒体转码产物。
- `P0-04` 已改为登录态下按订单号、PayPal Order ID 或 Capture ID 查询，并仅恢复与当前账号 ID/邮箱匹配的已支付订单，不再返回固定值。
- `P0-05` 已接入 PayPal 官方 JS SDK Buttons 和服务端 capture 接口，未配置公开 Client ID 时使用 PayPal 托管审批页跳转。
- `P0-06` 已增加已购拦截、待支付订单复用、客户端幂等键、审批地址持久化和数据库唯一索引。
- `P0-07` 已增加超级管理员全额退款、退款请求记录、PayPal 退款调用、退款/撤销 Webhook 同步、失败回滚、审计日志和退款完成后的权益回收。
- `P0-08` 已实现 start/heartbeat/complete 上报、断点续播、账号级观看进度和服务端清除历史。
- `P0-09` 已补目标国家内容过滤和播放 API 的 Origin/Referer 同源校验；CDN 边缘验签、分布式频控与并发设备限制仍需配套 Worker/KV 实现。

上述能力需要先执行 `migrations/0008_order_idempotency.sql`、`migrations/0009_refunds.sql`，并配置 Cloudflare 媒体地址、签名密钥和 PayPal 环境后才能进行真实支付/播放验收。

最关键的上线阻塞项是：真实 HLS 播放、视频上传与转码、生产内容/分集数据模型、真实购买恢复、退款闭环、支付防重复下单、观看进度同步、地区限制和防盗播/限流。上述能力未完成前，无法通过需求文档第 15 章的 MVP 验收。

| 优先级 | 数量 | 含义 |
| --- | ---: | --- |
| P0 | 9 | 阻塞试看、购买、解锁、播放或内容上架主链路 |
| P1 | 14 | 核心运营、合规、后台权限或用户体验不完整 |
| P2 | 5 | 性能、测试、监控和上线保障缺口 |

## 2. 已具备的基础能力

以下能力已有真实代码基础，不应判定为完全缺失：

- 用户注册、登录、退出、Cookie 会话以及前台路由登录拦截。
- 管理员登录、超级管理员账号管理、管理端路由/API 鉴权。
- 首页、探索、详情、锁片页、片库、个人中心及主要后台页面。
- D1 中的用户、订单、权益、播放事件、Webhook 事件和审计日志基础表。
- PayPal Orders API 创建订单、return capture、Webhook 官方验签、金额/币种校验和重复 Webhook 防重复处理。
- 播放接口的登录/权益校验和 10 分钟 HMAC 地址生成逻辑。
- 后台订单查询、对账汇总、用户禁用、手工补权益、首页分区、分类标签和域名配置界面。
- 当前代码通过 `npm run typecheck`。

## 3. P0：上线阻塞功能

### P0-01 真实视频播放器未实现

**需求对应：** 5.4、9.1、9.2、15.1、15.3。

当前播放页只有海报背景和模拟控制层，没有 `<video>`、HLS 播放器或原生 HLS 接入。`getPlayback()` 返回的 `signedUrl` 没有赋给任何媒体元素；进度固定为 `18%`，时间固定为 `0:24`，音量、字幕、倍速、拖动、全屏只是界面控件。重试操作也不会重新获取签名地址。

**代码证据：**

- `pages/watch/[slug]/[episode].vue:13` 使用固定进度。
- `pages/watch/[slug]/[episode].vue:26` 获取授权后只记录播放开始，没有消费 `signedUrl`。
- `pages/watch/[slug]/[episode].vue:47` 播放画面实际是背景图。
- `pages/watch/[slug]/[episode].vue:57` 至 `59` 为静态时间和未接入媒体对象的控制按钮。

**完成标准：** 接入真实 `<video>` 和 HLS 能力，使用服务端签名地址播放；实现播放/暂停、拖动、音量、清晰度、倍速、全屏、下一集、签名续期、断网重试及可读错误状态。

### P0-02 视频上传、校验、转码和媒体存储链路未实现

**需求对应：** 7.1、9.1、9.2、15.2、15.3。

后台上传入口明确提示尚未配置媒体上传与转码 Worker，且仓库中不存在上传凭证 API、分集上传 API、断点续传、格式/轨道校验、转码任务、R2/Stream 写入、HLS 多码率生成、截图或预览图生成逻辑。

**代码证据：**

- `pages/admin/series.vue:130` 直接提示“文件未上传”。
- `pages/admin/series.vue:186` 至 `189` 的分集列表和清晰度信息是展示层模拟。
- `server/api` 中没有需求建议的 `episodes/upload`、转码回调或媒体任务 API。

**完成标准：** 建立原片私有上传、单集/批量/断点续传、媒体校验、异步转码、480p/720p HLS、任务进度、失败重试、封面截图和发布前预览闭环。

### P0-03 生产内容与分集数据模型未实现

**需求对应：** 7.1、11.1、11.2、12.2、15.2。

当前 D1 迁移没有 `series`、`episode`、`category`、`tag` 等内容表。短剧、分集、分类和域名由 `data/mock.ts` 初始化，再整体序列化到通用 `home_config.payload`。新建短剧默认没有分集，而当前又没有可用上传链路，因此无法通过后台真正完成“新建短剧 -> 上传分集 -> 转码 -> 上架”。

**代码证据：**

- `server/utils/managed-content.ts:2` 内容源仍依赖 `data/mock.ts`。
- `server/utils/managed-content.ts:63` 至 `76` 将业务对象作为 JSON 存入 `home_config`。
- `server/utils/managed-content.ts:142` 至 `148` 新建短剧为 0 集、空分集数组。
- `migrations/` 只有用户、订单、权益、播放事件、管理员和通用配置表，没有内容核心实体表。

**完成标准：** 增加规范化的短剧、分集、分类、标签、内容关联、媒体资源和发布版本表；补齐唯一约束、软删除、订单关联保护及内容迁移脚本。

### P0-04 “恢复购买”是固定返回值，没有真实校验

**需求对应：** 5.4、5.6、8.2、15.1、16。

恢复接口不检查登录用户、订单邮箱、订单号、PayPal 交易号或支付状态，也不写入/迁移权益，任何非空输入都会返回恢复 2 笔。这既无法恢复真实权益，也不满足二次校验要求。

**代码证据：** `server/api/orders/restore.post.ts:3` 至 `6`。

**完成标准：** 仅允许登录用户调用；对订单号/PayPal 交易号进行支付事实核验；邮箱恢复必须有验证码或等价二次校验；恢复结果应幂等地关联到当前账号并记录审计/安全日志。

### P0-05 PayPal 前端接入和支付状态闭环不完整

**需求对应：** 5.5、8.1、8.2、15.1。

解锁弹窗使用自绘的 PayPal 样式按钮，违反“使用官方 SDK 或官方托管按钮”的明确要求。弹窗没有真正覆盖取消、成功、回调延迟超时和联系客服状态；支付返回后仅在详情页轮询约 15 秒，超时后没有持续查询或恢复入口。

**代码证据：**

- `components/UnlockSheet.vue:88` 至 `92` 使用 `paypal-demo-button` 自绘按钮。
- `components/UnlockSheet.vue:97` 至 `108` 只有处理中/成功展示，且当前跳转流程不会在弹窗内走到成功状态。
- `pages/series/[slug].vue:13` 至 `21` 仅轮询 10 次。

**完成标准：** 接入 PayPal JS SDK/Hosted Buttons；覆盖 pending、processing、paid、failed、cancelled、refunding、refunded、risk_review；回调延迟可继续查询并提供重试、订单入口和客服入口。

### P0-06 下单防重复扣款与待支付订单续付未实现

**需求对应：** 5.3、8.1、8.2、11.2、15.3。

每次点击购买都会创建新的本地订单和新的 PayPal Order，没有先检查当前用户是否已有有效权益，也不会复用仍可支付的 pending/processing 订单。因此快速重复点击或已购用户再次请求接口时，存在多笔真实付款风险。当前幂等仅覆盖同一 PayPal Order 的 capture/Webhook，不覆盖“同一用户重复创建不同订单”。

**代码证据：** `server/api/orders/index.post.ts:19` 至 `42`。

**完成标准：** 服务端创建订单前检查权益和同剧未完成订单；已购直接返回权益状态；未支付订单返回原订单继续支付；增加业务幂等键、价格版本/活动快照及并发唯一约束。

### P0-07 退款、退款 Webhook 和权益回收闭环未实现

**需求对应：** 7.4、8.2、8.3、15.2、16。

当前只有退款相关状态字段和查询展示，没有发起/记录退款 API、客服处理结果、PayPal refund 调用、退款 Webhook 处理、失败重试或退款确认后的权益回收。Webhook 只处理 `PAYMENT.CAPTURE.COMPLETED`。

**代码证据：**

- `server/api/paypal/webhook.post.ts:23` 仅处理支付完成事件。
- `server/api/admin/orders/[orderNo]/verify.post.ts` 仅同步已完成 capture。
- `server/api/admin/pending-items.get.ts` 能展示退款中数量，但没有处理动作。

**完成标准：** 支持退款申请/人工记录/官方退款结果同步；处理 refund 相关 Webhook；退款状态与权益状态分离；按默认策略在退款确认后撤销权益，并保留全量审计记录。

### P0-08 观看进度与观看历史没有形成闭环

**需求对应：** 5.4、5.6、11.1、12.1、14.1、15.1。

服务端可以接收 start/heartbeat/complete，但播放页只提交一次 `start`，位置和时长均为 0；需求中的 `/me/watch-history` 读写 API 不存在。片库通过最后一条播放事件推算进度，因此当前无法得到有效的继续观看位置。个人中心“清除历史”只在当前页面内隐藏项目，刷新后恢复。

**代码证据：**

- `pages/watch/[slug]/[episode].vue:27` 至 `30` 只发出 start 事件且进度为 0。
- `server/api/me/library.get.ts` 从播放事件推算进度，但没有专用历史模型/API。
- `pages/profile/[section].vue:103` 至 `105` 仅修改本地数组。

**完成标准：** 播放过程中定时写入 heartbeat，暂停/离开/播完时持久化位置；支持按账号读取、继续播放和服务端清除历史；跨设备恢复最近集数和秒级位置。

### P0-09 地区控制和基础防盗播能力不完整

**需求对应：** 3.1、5.3、5.4、9.3、13、15.3、16。

目前只有服务端权益校验和 HMAC URL 生成。内容查询会直接移除 `targetRegion` 字段，但不会按 IP/国家过滤；签名播放地址未绑定会话、IP 或设备，仓库中也没有 CDN 侧验签、Referer/Origin 校验、频控、并发设备限制、异常下载识别和试看防滥用逻辑。更关键的是，前端当前没有真正使用签名 URL。

**代码证据：**

- `server/utils/managed-content.ts:95` 至 `97` 只按上架状态过滤，未按地区过滤。
- `server/api/playback.get.ts:38` 至 `42` 签名 URL 只包含路径、用户和过期时间。
- 仓库中没有 rate limit、并发播放会话或 CDN 验签 Worker。

**完成标准：** 根据 IP 和版权配置控制可见/可售/可播；增加 CDN/边缘验签；绑定用户会话并按策略绑定 IP/设备；实现 Origin/Referer 校验、接口频控、并发限制和异常行为记录。

## 4. P1：核心功能不完整

| ID | 模块 | 缺失或仅部分实现的功能 | 当前证据/影响 |
| --- | --- | --- | --- |
| P1-01 | 探索页 | 服务端类型/语言/标签筛选和 Popular/Newest/Most Watched/Most Purchased 排序 | `server/api/explore.get.ts` 只处理关键词；页面在客户端对一次性全量数据排序，“Most Purchased”只是按当前用户是否已购排序，不是全站购买量。 |
| P1-02 | 首页 | 下拉刷新、分页/懒加载、返回滚动位置、真实分类切换 | `pages/index.vue` 的 Tab 只滚动到固定分区，Rankings 复用 Popular，Categories 固定跳 Romance；没有下拉刷新和分页。 |
| P1-03 | 首页运营 | 配置的内容来源策略未生效 | 后台可选择“按更新时间/收入自动排序”，但 `server/api/home.get.ts` 始终只按 `itemIds` 取内容，`source` 字段没有参与计算。 |
| P1-04 | 详情页 | 相关推荐和可用的分享功能 | 详情页没有推荐短剧区域；分享按钮没有事件处理。 |
| P1-05 | 短剧资料 | 完整内容字段和素材维护 | 缺少英文名/别名、短标题、导演、版权、语言/字幕、解锁范围策略、SEO 标题/描述/分享图；封面和横幅不能上传，创建时使用固定示例图。 |
| P1-06 | 发布管理 | 定时上下架、发布预览、恢复上架、软删除和有订单内容保护 | 当前状态切换会立即生效；`publishAt` 被当成更新时间写入，没有调度任务、删除接口或发布版本预览。 |
| P1-07 | 批量运营 | 批量编辑分类/标签/试看数/价格 | 当前批量操作仅支持状态变更，未支持需求中的批量字段编辑。 |
| P1-08 | 价格与活动 | 原价、活动价、地区价、有效期、折扣码、次数上限、税费和最终金额展示 | 后台只可维护单一 USD 价格；订单没有价格版本/活动快照；“包含未来更新”在前台硬编码，不能按剧配置。 |
| P1-09 | 分类标签 | 配置与前台实际展示未打通 | 探索页分类数组硬编码；短剧后台没有标签编辑字段；标签颜色、启用状态和有效期不会改变前台 badge。 |
| P1-10 | 用户中心 | 账号级语言/隐私设置、真实数据导出/删除流程、完整用户详情 | 语言和隐私只存 `localStorage`，不会切换页面文案；导出的只是本地偏好；后台用户详情只显示订单/权益数量，没有观看记录、订单明细和退款明细。 |
| P1-11 | 全局配置 | 站点名、Logo、客服邮箱、服务条款、隐私政策、退款政策 | 页面品牌和邮箱均硬编码；没有 `site-config` API 或后台编辑页。 |
| P1-12 | 后台权限 | 内容运营、财务/运营管理员的细粒度 RBAC | 当前只有 `admin` 和 `super_admin` 两类；普通 admin 可访问内容、用户、订单、系统查询等全部普通接口，未按岗位最小授权。 |
| P1-13 | 系统/域名 | 后台切换 PayPal 环境、真实自定义域名绑定、证书申请和 301 执行 | 系统页明确是只读检查；域名模块只保存配置、查 CNAME/HTTPS，未调用 Cloudflare Custom Hostnames/路由配置，也没有真正执行旧域名 301。 |
| P1-14 | 审计/API | 审计前后值和统一错误响应 | 审计表只有文本 `detail`，没有需求要求的 `before_json`/`after_json`；只有成功响应使用统一 Envelope，`createError` 错误未统一保证 `code/message/request_id`。 |

## 5. P2：质量与上线保障缺口

| ID | 模块 | 缺失功能 | 影响 |
| --- | --- | --- | --- |
| P2-01 | 埋点与指标 | 首页/卡片曝光点击、详情、试看完成、锁片、支付、搜索筛选分享、首帧/卡顿/失败等事件 | 当前只有播放事件 API，且前端只发送 start；无法计算需求第 14 章的大部分核心指标。 |
| P2-02 | 图片与性能 | WebP/AVIF、响应式图片、首图优先、接口降级、性能指标采集 | 海报均为固定 JPG，未发现图片变体或 LCP/首帧/卡顿监控。 |
| P2-03 | 自动化测试 | 单元、API 集成、支付幂等、权限、安全和端到端业务测试 | 仓库只有截图/基础交互 `visual-check`，没有测试框架用例覆盖需求第 16 章。 |
| P2-04 | 兼容与网络验收 | iOS Safari、Android Chrome、内嵌浏览器、弱网/断网、美国节点测试 | 当前自动化只使用桌面 Chrome 内核，不能证明移动浏览器和目标网络达标。 |
| P2-05 | 生产运维 | 日志告警、Webhook 自动补偿任务、备份、监控、应急回滚 | 有后台状态展示，但没有定时补偿 Worker、告警、备份/恢复和发布回滚实现。 |

## 6. MVP 验收影响

| 需求验收项 | 当前判断 | 主要原因 |
| --- | --- | --- |
| 登录后浏览首页、分类、详情 | 部分通过 | 路由鉴权和页面存在，但分类/地区/首页策略不完整。 |
| 按配置播放试看集 | 不通过 | 没有真实视频播放器和媒体文件链路。 |
| 锁片后 PayPal 支付并解锁 | 部分通过 | 后端已有 PayPal 基础链路，但官方按钮、重复下单防护和完整状态处理不足。 |
| 刷新后权益仍存在 | 部分通过 | 支付成功可写 D1 权益，但退款、恢复购买和重复订单场景不完整。 |
| 用户查看订单、已购和观看记录 | 部分通过 | 订单/权益可查，观看位置与清除历史不真实。 |
| 后台上传、转码、预览和上架 | 不通过 | 上传/转码/分集管理未实现。 |
| 后台完整维护价格、活动、分类标签 | 不通过 | 仅支持基础价格和孤立的分类标签配置。 |
| 后台订单、退款、对账 | 部分通过 | 查询和 CSV 已有，退款处理闭环缺失。 |
| 域名切换不丢数据并真正迁移流量 | 部分通过 | 数据与域名基本解耦，但没有实际绑定、证书和 301 执行。 |
| 短时签名、防盗播和异常限流 | 不通过 | 只有基础签名生成，播放器/CDN 验签/限流未形成闭环。 |
| 高风险操作完整审计 | 部分通过 | 有操作者、时间、IP 和摘要，但缺少变更前后结构化值，部分尚未实现的高风险动作也无日志。 |

## 7. 建议实施顺序

1. **先补内容与媒体底座：** 建立 `series/episode/media_asset` 等数据表，完成 R2/Stream 上传、转码 Worker、回调和真实 HLS 播放。
2. **再闭合播放与历史：** 把签名地址接入播放器，实现签名续期、heartbeat/complete、跨设备进度和历史清除。
3. **收紧支付正确性：** 使用官方 PayPal 前端组件，增加已购判断、待支付复用、业务幂等键、价格快照和回调补偿。
4. **完成恢复与退款：** 实现二次校验的恢复购买、退款事件同步、人工处理记录和权益撤销策略。
5. **补齐运营能力：** 完整短剧字段、素材、定时发布、活动价格、首页自动来源、分类标签联动和全局站点配置。
6. **完成安全与上线验收：** 地区版权控制、CDN 验签、限流/并发限制、结构化审计、兼容/安全/性能测试和监控回滚。

## 8. 本次评估边界

- 本报告判断的是仓库内可见实现，不代表外部 Cloudflare、PayPal 商户、CDN 或生产域名当前实际配置状态。
- 未将需求明确列为非目标或可隐藏的功能计入缺口，例如订阅、评论、私信、社交关注以及 MVP 可隐藏的收藏入口。
- 多语言内容本身不是当前 MVP 验收项，但“文案使用可配置翻译资源、不得在组件中硬编码”属于架构要求，因此相关配置能力仍列为 P1。
- `npm run typecheck` 已通过；由于本任务是功能盘点，未执行真实 PayPal、视频、Cloudflare 或多设备端到端测试。
